{
  "schemaVersion": 1,
  "projectId": "atomic-acres-browser-arena",
  "repository": "daveinturkey15-byte/atomic-acres-browser-arena",
  "registrySchemaVersion": 1,
  "registryExample": "docs/PROJECT_ROUTING.example.json",
  "documentation": "docs/PROJECT_ROUTING.md",
  "purpose": "The one stable identity a launcher must name before scripts/release/pipeline-guard.mjs will route a worktree. The machine-local registry that binds this identity to a Git database, worktrees, integration, preview, production and rollback is never committed; see documentation."
}
