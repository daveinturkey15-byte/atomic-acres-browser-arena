# Day plan 2026-09-03 (owner away ~06:20-15:20 BST; usage at 80%: paced, at most two Opus chains at a time, one cut per block)

Orchestrator: Claude Code (Fable 5.1). Every worker Opus 5.1. Owner statement
(06:15): "what are the actual future plans for today? i will be away for 9 hours,
so we can slowly but surely work on things but using alot of compute" after
"we are at 80% of usage so maybe need to take it a bit more careful today on
volume". Read together: steady, serial-ish work, no waves; stop launching if the
owner says the window is tight.

| Block | Window | Chains | Work | Exit |
|---|---|---|---|---|
| 1 | 06:20-07:30 | 1 | **PASS 89**: merge AI (Balanced + RTX explainer + ladder doc), AB (time of day), AL (lighting tiers; re-check its two runtime blockers are fixed), trials AP/AM/AO (AN stays out until its per-profile frame table exists); integration worker resolves the roster/corridors conflicts; then the cut ritual | PASS 89 live, PASS 88 backup |
| 2 | 07:30-10:30 | 2 | **Farcrysis to a real arena** (material collapse 224 -> shared vocabulary, core building floor/walls + practical light, the 25 runtime eye rows, the owner's vegetation technique) and **the animation job** (third-person operator gets reload/crouch/prone/ADS/knife via the Kimodo clips PASS 80 measured shippable; bots and peers play them) | both merge-ready with skeptic verdicts |
| 3 | 10:30-13:30 | 2 | **Raid Rebuild art pass** (brief-driven method + `open-world-city-art-loop` for the drive/street, three critics on captures) and **water everywhere** (pond in every level from the Map 3 module, re-point High Seas/Raid/Nuke Town oceans, roster test green) | both merge-ready |
| 4 | 13:30-15:00 | 1 | **PASS 90** cut with whatever is green; hill-climb loop (AG) staged with first receipts for the owner to read; governance chore (AKP/vault commits by explicit paths, `akephalos sync`) | PASS 90 live; receipts |
| deferred | with the owner | - | Lane AC branch/worktree cleanup (402 worktrees), the three preview promotions, Balanced default, Raid art direction (warm quadrant full), SAM licence, Lane T re-land, H2's +488 ms switch residual, AN merge | - |

Standing rules as every lane (headless only, never kill the owner's processes,
ComfyUI queue empty for measurements, explicit-path commits, never weaken a
gate). Cut ritual: `roll_pass.py --pass N`, full vitest, build LAST, Farcrysis
receipt against the bundle, boot smoke all arenas, dry-run, publish, live check.
