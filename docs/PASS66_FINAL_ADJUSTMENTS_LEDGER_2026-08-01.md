# Atomic Acres — Pass 66 final-adjustments ledger

Date: 2026-08-01  
Status: **ACTIVE CANDIDATE — NOT APPROVED, NOT PUBLISHABLE**

This is the single coverage ledger for the owner's final-adjustment report. A row is not complete because code exists or an older note says it is complete. `FIXED` means the current candidate has a focused mechanical regression. `CANDIDATE` means the latest integration branch contains a plausible implementation that still needs the stated current-SHA verification. `OPEN` means the requested result is absent or the owner has rejected the existing result.

## Release truth

- Public Pass 66 channel source: `ca24d4dbbc5c15491be7e7d32f5ce84ecb1f4fff`.
- Public Pages commit: `7c648793e5c4ce61b06572d980fb64413c0f11e2`.
- Latest recovered Pass 66 integration source: `eb813af228bd31fefce90ffaa1078372480328c0`.
- This candidate branch: `contrib/dave-gaming-pc/codex/pass66-final-adjustments-20260801`, based on `eb813af228bd31fefce90ffaa1078372480328c0`.
- The public Pass 66 channel is therefore missing hundreds of later integration commits. It remains untouched while this candidate is corrected and verified.
- Byte-exact Pass 63 remains the stable rollback. No replacement release is authorized by this ledger.

## Owner report coverage

| Area | Status | Current evidence / required next proof |
|---|---|---|
| M40 ADS blanks/whites out | **FIXED** | Scope mode forced every HUD child to full opacity, including the full-screen death/respawn fade. The candidate removes that override. `pass66-scoped-ads-regressions.spec.ts` rejects opaque white and black centre layers and proves frame advance at 2560×1440 and 3840×2160. |
| All guns use the same little red ADS circle | **FIXED** | `ads-sight-profile.ts` projects a catalog-complete, distinct ADS marker signature for every canonical weapon. Unit set-equality/uniqueness and served centre-ray coverage are present. Full native-WebGPU visual review remains required before owner approval. |
| M14 EBR freezes on ADS | **VERIFIED CANDIDATE** | The post-live integration prewarms the M14 thermal pipeline. Installed Edge hardware WebGPU at 2560x1440 now enters and leaves ADS through trusted right-mouse input with 124 presented frames over the 1.5 s hold, zero runtime/GPU errors and no white frame. Repeat on the frozen SHA and across the remaining map/profile tails before release. |
| Railgun lost its through-wall scope | **VERIFIED CANDIDATE** | Installed Edge hardware WebGPU now proves the restored cyan thermal contact and 3D silhouette through a confirmed solid wall using trusted right-mouse input, with depth bypass active and advancing presentation. Re-run the retained three-peer authority gate on the final SHA. |
| Field Kit custom cards unreadable until hover | **FIXED** | Persistent Custom 1/2/3 cards now have a dark high-contrast base, readable labels/stats/DPS and selected state without hover. Unit and Chromium menu tests pass; 1600×900 evidence captured. |
| Weapon pickup then re-pick swapped weapon | **CANDIDATE** | Post-live integration contains the death-drop re-pick correction; focused death-drop/pickup tests pass. Re-run the complete served pickup path, including RustRig crates, on final SHA. |
| Arms/hands/knife too small or poor at 1440p/4K | **VERIFIED CANDIDATE** | The authored two-chain arms and wrist-mounted knife now pass strict paused and temporal 1440p/4K/ultrawide framing: opaque connected hands, 30 articulated finger bones, sub-0.001 mm ADS contact, more than 80 mm receiver-depth clearance, visible knife/grip contact, and materially distinct reload/melee poses. A real-wall Blender-quality browser gate and 108 focused tests pass with no runtime errors. Owner HITL acceptance remains required; this is a stable stylized first-person result, not a claim of photoreal anatomy. |
| Prone/wall/floor viewmodel clipping | **VERIFIED CANDIDATE** | The real-wall browser path now proves 0.5924 m obstruction retreat and 0.1342 m floor lift while prone, with finite near-plane-clear framing at 1440p, 4K and ultrawide. Unit/action/state coverage and the strict browser test pass; final owner HITL still decides whether an additional map-specific contact case is visible. |
| Chopper/care/carpet silhouettes and Apache-level detail | **VERIFIED CANDIDATE** | The bounded V4 Blender pass now provides a 59,740-triangle armed attack helicopter with layered canopy/side/dorsal armour, fasteners, louvers, gun and rocket stores; a distinct four-engine heavy care aircraft with framed flight deck, gear and rear cargo ramp; a panelled flying-wing bomber with buried intakes and framed bay; and a latched/tied-down crate. All eleven LOD GLBs remain within the retained ceilings, canonical sockets/orientation are unchanged, 29 focused tests and the real slot-key-6 Chopper browser path pass, and bright close-up evidence exists. The final prerecorded cockpit preview must be recaptured against the new source hash; owner visual approval remains required. |
| Chopper Gunner does no damage | **VERIFIED CANDIDATE** | The real slot-key/pointer-lock/mouse-hold browser path now reduces a moving named dummy's HP with exact Chopper activation attribution. The defect was a low-FPS pose-order race: AI flight moved the host ray origin after accepting the player's camera-space intent. Damage now resolves from the coherent accepted pose before flight integration while host LOS/cadence/replay gates remain intact. |
| Piloted Drone/Chopper instruction too large | **VERIFIED CANDIDATE** | The duplicate bold banner is removed. Chopper and Piloted Drone now highlight the existing assigned-key action row inside the top support-information panel; a live Chromium matrix proves one surface, readable 10–13 px type, no other HUD-console overlap and material rendered pixels at 700×720, 960×540, 1280×720, 2560×1440 and 3840×2160. Repeat through the exact-SHA owner gate after freeze. |
| Killstreak descriptions | **VERIFIED CANDIDATE** | The loadout panel has complete typed set-equality with the selectable catalog. Yardhawk truthfully describes the homing explosive; Piloted Drone and Chopper use the assigned-slot-key operation contract; Nuke projects the canonical five-second global warning rather than claiming instant effect; and Drone Swarm projects one 30 s lifetime through catalog, runtime and menu. Semantic/unit coverage passes; the final viewport capture remains. |
| Killstreak grey-room gallery thumbnails | **IN PROGRESS** | The off-range test bay and real runtime capture route exist. Eleven unique game-rendered poster frames are present, but their older poster-only receipt is not release evidence. The replacement capture gate is wrapper-only and binds a clean full SHA, a newly owned staged preview, exact served candidate SHA/runtime tree, recursive `src/**`/`shared/**`/`public/**` inputs, build/env/topology recipes and exact ID-to-media paths. Release verification permits only a clean descendant containing added/modified finalizer-owned media/provenance. Final H.264 clips remain deliberately uncaptured until source freeze. |
| Gun Range tunnel, five-second corridor, secure thump door, large test bay, slow unarmed dummies, all weapons/supports | **VERIFIED CANDIDATE** | `gun-range-test-bay.ts` owns a 30.75 m / 5 s corridor, proximity thump door with real collision, 3,104 m² bay, four slow unarmed dummies, and canonical weapon/support stations. The unit/integration surface is green; the served browser gate and final visual review remain exact-SHA requirements. |
| Flamethrower on top of RustRig halfway through match | **VERIFIED CANDIDATE** | A full pickup-only weapon family now projects through protocol v13, catalogs, bots, audio, authored GLBs/LODs/textures, provenance, ballistics, stream presentation, timed host authority and reconnect checkpointing. The real two-peer RustRig mid-match spawn/rejoin case is green; repeat asset and exact-SHA gates before release. |
| Flare gun in Terminal aircraft halfway through match | **VERIFIED CANDIDATE** | The full pickup-only family, finite ammo, projectile/effect authority, audio, authored assets/provenance, timed aircraft spawn and active-flare late-join repair are integrated. It now also projects as bot-eligible through the canonical bot/GPU catalog, remains excluded from ordinary spawn kits, and has a dedicated gravity-compensated host projectile adapter; the bot path cannot fall through to hitscan. The real two-peer finite-ammo/depleted-rejoin and active-projectile late-join cases are green; repeat on the frozen SHA. |
| Skybox quality across every map / Acres sky still poor | **VERIFIED CANDIDATE** | Atomic Acres, RustRig and Terminal now own separate restored 4096×2048 equirectangular WebP skies with preserved sources, exact hashes and source-relative detail/entropy/seam gates. The invalid mirrored/black authoring attempts were rejected; admitted sources were restored through the pinned Real-ESRGAN x4 pipeline, periodically feathered and encoded only after the quality gates passed. Legacy RustRig point-star overlays and giant WebGPU cloud planes are retired, so the panorama is the sole outdoor sky owner. Final WebGL2 and installed-Edge hardware WebGPU (`nvidia / blackwell`) matrices cover every outdoor map at 3840×2160, player-height Atomic context and ± seam-wrap views with one arena root, healthy watchdog/presentation, no device loss, no uncaptured errors, no square stars, no flying geometry and no visible hard seam. Owner HITL approval remains required. |
| RustRig big volatile physical waves | **VERIFIED CANDIDATE** | Rendering and CPU physics share one deterministic five-direction warped 22-180 m storm spectrum at 1.55 m amplitude, including analytic normals, vertical velocity, buoyancy and drag. The WebGPU surface now uses 256x256 displacement cells, bounded crest breakup and a curved 3.2 km horizon instead of flat 10 m facets or a visible plane edge. Installed Chrome/NVIDIA WebGPU deploys active with no device/runtime errors; owner visual violence/performance review remains. |
| Loading stalls, half-load then instant second Join | **FIXED CANDIDATE** | The observed hard blocker was authoritative lobby ping/score telemetry incrementing the revision during cold preparation, repeatedly superseding the same countdown and leaving a committed arena paused. Admission now coalesces telemetry-only revisions and countdown-to-active updates only when role, room, connection epoch, arena and both start clocks are identical; waiting/ended/map/clock/authority changes still abort. Installed Chrome/NVIDIA WebGPU cold RustRig reaches active gameplay in 27-39 s with advancing frames and no runtime/GPU errors. Frozen-SHA multiplayer, hidden-tab/endurance and real Firefox evidence remain before release. |
| Chrome/Firefox/Edge/Safari compatibility | **PARTIAL FIX** | Installed Edge WebGL2 and native WebGPU, Chromium, and Playwright WebKit deploy-to-active smokes pass. Stock Firefox 153 and Playwright Firefox 151 both crash while creating a blank page in this Windows environment (`RenderCompositorSWGL` default-framebuffer failure) across default/basic/hardware/headless and isolated hidden-headful attempts, before Atomic Acres loads; Firefox product compatibility therefore remains unverified rather than failed. |
| Client/player and host/server stack traces | **VERIFIED CANDIDATE** | Bounded, redacted full browser stacks are captured into the local technical match JSON for both host and guest roles, with a short trace in the live runtime error panel. The Cloudflare service catches request failures, writes a bounded provider-side stack with an incident ID, and returns only that incident ID—not the stack—to the player. Uploaded match diagnostics deliberately exclude stacks. Client, diagnostic and Worker suites pass; provider log retention/access remains an operational policy rather than game code. |
| Host crash then rejoin / stuck lobby | **FIXED CANDIDATE** | Protocol v12 and checkpoint schema v3 now resume the same active room and host identity, position, match epoch, exact bots, guest pose/loadout/host-owned health, compact host-owned ordinary magazine/reserve/grenade inventory, finite railgun holder/ammo/replay state, both active phases, both remote replicas, connected roster, scores and reliable repair commits. The final current-worktree owned-production-topology case depleted two sniper rounds and one grenade, crashed the host renderer, recovered the host, introduced a fresh wrong-kit guest document, and proved exact inventory/HP/loadout/railgun convergence. A client host-liveness watchdog detects falsely-open zombie WebRTC lanes while preserving hidden-tab/focus grace and reuses the existing 90-second credentialed reconnect path. Checkpoint writes remain on a two-second cadence, so a hard crash can roll back inventory changes made since the most recent successful checkpoint. This remains candidate evidence; repeat on the frozen SHA. |
| Guest crash/rejoin and late-join desync | **FIXED CANDIDATE** | Protocol v12 retains epoch-paired event/state lanes and adds two-phase admitted transport replacement: a higher-generation same-token candidate stays provisional beside the admitted transport; only exact asynchronous credential confirmation promotes it and closes the prior lanes, while candidate rejection or close leaves the active transport intact. A reliable interactive-world snapshot is applied before resume authority, and the authority commit carries `worldRevision` so the client queues it until the matching world revision is applied. Browser scenarios use separate host/guest contexts and owned randomized PeerJS topologies. The full impairment set still has to pass on the frozen SHA. |
| Host can easily change map in lobby | **VERIFIED CANDIDATE** | The served two-browser gate now cycles RustRig → Terminal → Gun Range → Nuke Town → RustRig and, after every host selection, requires host/guest lobby equality, exactly one matching active scene root, matching navigation colliders, an enabled ready action and a disabled guest map selector. The matrix passes locally; repeat it on the frozen SHA. |
| Adrenaline Rush corrupts host lobby | **CANDIDATE** | Post-live source resets Adrenaline state across lobby/match epochs. Needs a host/guest rematch/rejoin regression. |
| Bots only work for host / guests cannot see results | **FIXED CANDIDATE** | Bot score rows remain in every host heartbeat and bot snapshots continue while the host player is dead. Host-authored bot damage/HP admission now keys from bot identity and replay authority independently of any lossy, missing or stale replica; replica gates suppress only cosmetic hit reaction. Host bot flamethrower stream hits/misses and flare launches still project through bounded presentation events so guests receive matching flame/flare visuals and audio without duplicate authority, tracers or impact sound. Focused protocol/replay/integration/audio tests pass; final separate-context impairment rerun remains. |
| Hosted bots differ from skirmish | **FIXED CANDIDATE** | Hosted bots now use the same canonical AI, firing and damage-scaling path as solo skirmish; guests buffer ordinary poses and snap only lifecycle discontinuities. Source-contract and live two-peer evidence are green; final HITL behavior comparison remains. |
| Slight orange highlight on other multiplayer players | **CANDIDATE** | Post-live source contains the requested remote-player highlight. Needs team/FFA/readability captures and no-through-wall proof. |
| Lobby and in-game chat clearer/better placed | **VERIFIED CANDIDATE** | Chat is persistent/discoverable in the lobby and a clearer bottom-left match surface. The served two-peer gate now enforces no overlap at 720p, 1080p, 1440p, 4K and 3440×1440 ultrawide; its final post-network-fix run and owner visual review remain. |
| Dane multiplayer JSON | **INPUT NEEDED** | The file is not in the repo or this task. Preserve the raw JSON locally, redact identity/free text, then compare its cadence/desync/shot/rejoin fields against the current diagnostics schema. |
| Shed door | **DEFERRED BY OWNER** | Explicitly excluded from this correction wave. |

## Verification run in this candidate

- AKP/pipeline contributor preflight: pass.
- TypeScript `tsc --noEmit`: pass.
- Final focused multiplayer repair suite covering protocol v13, checkpoint v3, two-phase transport replacement, world-revision-gated repair, bot HP decoupling, host-authoritative reload and compact host-owned guest inventory continuity: pass, 117 tests.
- Focused unit suite covering ADS profiles, menu contrast, rejoin identity, hosted lobby, prone physics, pickups, exception logs and support catalog: pass.
- Field Kit Chromium UI: pass, 4 tests.
- M40/M14 1440p + 4K scoped regression: pass; trusted right-mouse M40/M14/Railgun hardware-WebGPU HITL path also passes at 2560x1440.
- Chopper/drone authority coverage plus the real Chopper slot-key/pointer-lock/mouse-hold moving-dummy damage path: pass.
- Chromium WebGL2 deploy-to-active capability smoke: pass.
- WebKit WebGL2 deploy-to-active capability smoke: pass after poster-gate correction.
- Installed Edge WebGL2 capability smoke and native Edge WebGPU RustRig water gate: pass.
- Shared high-resolution sky/backdrop and seamless cloud-veil unit gates: pass.
- Four-arena long-run browser audio ownership gate: pass through 65 seconds per arena with the two intentional ambience sources/two spatial chains, bounded buses, advancing frames and no runtime faults.
- Native Edge WebGPU Atomic Acres sky/watchdog/map-switch receipt: pass; the final all-map native capture rerun is pending.
- Authored arms/knife/viewmodel paused and temporal matrices: pass at 1440p, 4K and ultrawide with zero violations; the real-wall Blender-quality browser case and 108 focused tests pass.
- Atomic Acres, RustRig and Terminal deterministic 2560×1440 WebGL2 sky/world capture gate: pass.
- Timed RustRig flamethrower, finite-ammo Terminal flare gun and active-flare late-join/rejoin real two-peer gate: 3/3 pass.
- Retained Pass 63 chat/guest-rejoin/hosted-bot real two-peer gate: pass after bot heartbeat repair.
- Final owned-production-topology Chromium host-renderer crash/rejoin gate: pass with two real peers, two depleted sniper rounds, one depleted grenade, host recovery, a fresh wrong-kit guest document and exact inventory/HP/loadout/railgun convergence.
- Scope boundary: this final multiplayer proof is from the current dirty candidate, not an owner-approved or frozen-SHA receipt; the two-second checkpoint cadence means a hard crash can roll back changes since the last successful checkpoint.
- Network chaos matrix: 1,000 clean, 1,000 normal and 1,000 adverse seeds with zero duplicate side effects.
- Firefox: stock 153 and Playwright 151 are blocked before page creation by the host compositor; not counted as a product pass or failure.

## Exact owner-evidence aggregate projection

The frozen-SHA owner-evidence runner now contains 70 non-optional catalog receipts. The following final-adjustment gates are first-class graph nodes rather than commands mentioned only in prose. Every package alias is pinned by the runner's static contract; the three focused browser specs without a dedicated feature alias use the canonical `qa:playwright-topology` launcher.

| Graph test | Exact aggregate command | Final-adjustment coverage |
|---|---|---|
| `T-MULTIPLAYER-STABILITY` | `npm run qa:pass66:multiplayer-stability` | Host crash/reclaim, guest replacement, lobby map/UI, hosted bots, timed-weapon rejoin, Qoder authority cases and Adrenaline rematch lifecycle. |
| `T-PASS61-NETCODE` | `npm run qa:pass61:netcode` | Retained seven-shot authoritative timing/damage agreement. |
| `T-PRIVATE-LOBBY` | `npm run qa:private-lobby` | Six-player admission, ready/start, rejoin identity and lobby convergence. |
| `T-PASS63-MULTIPLAYER-COMPARATOR` | `npm run qa:pass66:pass63-multiplayer-comparator` | Byte-exact Stable versus candidate chat, bots, replacement identity, repair and bounded timing. |
| `T-ADS-SIGHT-CATALOG` | `npm run qa:pass66:ads-catalog` | Every canonical weapon's distinct live sight plus M40/M14/Railgun ADS dwell on Edge WebGL2 and WebGPU. |
| `T-SCOPED-ADS` | `npm run qa:playwright-topology -- tests/e2e/pass66-scoped-ads-regressions.spec.ts --project=chromium --workers=1 --retries=0` | M40 whiteout and M14 frame-loop regression at 1440p and 4K. |
| `T-SKY-WEBGPU` | `npm run qa:pass66:sky-webgpu` | Outdoor arena sky ownership, map switching, watchdog/device health and visual captures. |
| `T-VIEWMODEL-FRAMING` | `npm run qa:pass66:viewmodel-framing` | Authored arms, hands, knife, hip/ADS/reload/melee/prone framing at 1440p, 4K and ultrawide. |
| `T-PRONE-CONTACT-MATRIX` | `npm run qa:pass66:prone-contact-matrix` | Twelve solo and twelve two-peer floor/wall contact cells. |
| `T-SUPPORT-OPERATE-PROMPT` | `npm run qa:pass66:support-operate-prompt` | One highlighted assigned-key action row inside the existing top information panel for both Chopper and Piloted Drone; no duplicate banner, no HUD-console overlap, and exact evidence across five viewports including 960×540, 1440p and 4K. |
| `T-HIDDEN-TAB-ADMISSION` | `npm run qa:pass66:hidden-tab` | Three-map background preparation with no hidden GPU/audio/authority work and one foreground recovery. |
| `T-AUDIO-LONG-RUN` | `npm run qa:pass66:audio-long-run` | Four arenas at 65 seconds each with real output-spectrum hiss rejection and bounded ownership. |
| `T-KILLSTREAK-DEMO-VIDEOS` | `npm run qa:pass66:killstreak-demo-videos` | Final eleven clean-SHA, real-test-bay videos and their recursive provenance closure; intentionally red until final MP4 capture exists. |
| `T-BROWSER-ADMISSION` | `npm run qa:pass66:browser-admission` | Installed Edge WebGL2/WebGPU and WebKit multi-profile admission cycles. |
| `T-GUN-RANGE-TEST-BAY` | `npm run qa:playwright-topology -- tests/e2e/pass66-gun-range-test-bay.spec.ts --project=chromium --workers=1 --retries=0` | Five-second tunnel, secure door/collision, live bay, dummies and weapon/support stations. |
| `T-TIMED-MAP-WEAPONS` | `npm run qa:playwright-topology -- tests/e2e/pass66-timed-map-weapons.spec.ts --project=chromium --workers=1 --retries=0` | Canonical midpoint spawn, finite flamethrower/flare behavior and pickup state. |
| `T-RAILGUN` | `npm run qa:pass64:railgun` | Through-wall thermal scope, rechamber, multi-hit authority, exact damage and observer replication. |

## Release blockers

1. The current arms/hands/knife/action and support-vehicle fidelity passes have fresh owner-visible evidence and mechanical acceptance; explicit owner HITL visual acceptance is still required.
2. The eleven final real killstreak MP4s must be captured only after those visual dependencies are frozen, then decoded/finalized through the clean-SHA, served-runtime and recursive-source-closure provenance gate. The existing poster-only schema is intentionally rejected.
3. Representative native-WebGPU cold/warm admission, all-map sky/ADS/railgun/support/endurance, hidden-tab return and complete multiplayer impairment gates must pass on one frozen SHA.
4. Installed Edge and WebKit are green, but Firefox cannot create a blank automated page on this machine and therefore still needs an independent real-browser HITL check.
5. Dane's diagnostic JSON has not been supplied.
6. No Pass 66 acceptance record or immutable preview receipt authorizes production promotion.
