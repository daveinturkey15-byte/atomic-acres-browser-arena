# Pass 81 overnight run — 2026-08-28 → 29

Branch `gauntlet/pass79-omp-20260823`, all pushed. Suite floor rose 4,501 → 4,507
(500 files) and every commit was verified against it. Nothing published yet — the
grass/mountains lane is still in flight and publishes gate on it.

## Landed, in owner-priority order

| Item | What a player feels | Evidence |
|---|---|---|
| **Farcrysis lock** | Cannot reach any live chooser | Publish guard reads the minified registry bytes (`selectable:!1`), runs its own red test on every publish after the first draft shipped vacuous |
| **HF-387 clip sweep** | Prone/wall camera clipping measured, worst fixed | New two-stage instrument, ~120k probes, 5 arenas. Nuke Town's storage lockers were shot surfaces with **no movement collider** (stand inside a cabinet) — now real solids, fragment derives from them. Ledger + `--check` ratchet wired as `qa:eye-clearance` |
| **Ballistics (agent lane)** | Wood shoots like wood; **ghost cover closed** | Premise inverted on measurement: unregistered meshes were fail-OPEN (bullets crossed silently). 22 ghosts closed, 100% triage all five arenas, 13 accepted rows with written reasons, zero-ceiling ratchet proven red-first |
| **HF-383b fins** | Street reads less bulky | Width 1.4→1.0 only — depth 4.8 measurably reopened a 54 m lane (gate went red), so depth stayed. 60/60 arena gates green, art rebaked, provenance re-pinned |
| **HF-384 railgun** | Can't clock-camp the rare weapon | 150 s ± 30 s, deterministic from the replicated site seed (golden-ratio decorrelated so room ≠ time), checkpoint restore re-pinned |
| **HF-385 overdrive** | 2× contests more often, reigns shorter | 90 s / 20 s, multiplier untouched, one-constant reverts |
| **HF-382 stance** | Squad actually sees your carry | Full lobby replication mirroring the skin path; live rigs cross-fade mid-match |
| **Emote (new)** | **B plays Wave/Fist/Boot on your rig for others** | First-ever new bindable action; fixed the resolver silently discarding custom binds; bounded one-shots; host-validated relay; 1.5 s local cooldown |
| **HF-347 wedge** | "Synchronizing…" can't lie forever | 75 s deadline on its own clock (stalls also stall the renders), one auto-reselect, then honest LEAVE guidance |
| **HUD sway** | No frozen lean through pause | Lifecycle-transition release — the frame-loop latch can't run on the exact frames a pause begins |
| **Chopper `× ×3`** | Single glyph | Verified already fixed by wave 1; contract counts total glyphs |

## The ratchet fired once — and was right

First eye-clearance run after the ballistics merge: atomic 0 → 24. Every row a
surface the probe **couldn't see** before ghosts were registered (ramps, greenhouse
panes); other four arenas unchanged 3/0/2/2 — the control separating new sight from
new damage. Ledger re-baselined with cause and itemisation, never silently.

## Open

- **Grass + mountains lane** (agent, in flight) — merge → fps/screenshot verify → publish
- **N2 topology fidelity** — held for your eye; three Nuketown changes tonight already, a fourth before you play muddies feedback
- Ramp-flank clip fringe (≤ 0.142 m, navigation-coupled) — deferred with numbers in the ledger
- Owner questions parked by the ballistics lane: coral-house brick family; coach window band as real aperture; high-seas hot-tub rims authored `reinforced`

## Tomorrow list (unchanged)
Operators with real sculpted models (roster word needed) · Kimodo runtime wiring + IK foot-lock · RTX close-out · farcrysis repair + TSL retrofit · decomposition discussion
