# HF-421 evidence — see the lane-named folder

The pass/fail bar named this path; the trial's rules named the lane-named one. The
evidence lives once, in

    docs/evidence/pass86/an-subway-scene-lighting-look/

(`MEASUREMENTS.md`, `readability.json`, `hud-before.json`, `hud-after.json`,
`pipeline-compile-map3-after.json`, `captures/`). This file exists so a reader who
follows the bar's path finds it rather than an empty directory.
