# Candidate 8 review — Muse Spark 1.3 (third pair of eyes, pre-PASS-95)

- Candidate branch: `origin/contrib/dave-gaming-pc/claude/pass93-candidate`, served head `4b5cc28b`.
- REPORT under review: `docs/evidence/pass94/candidate8/REPORT.md` (commit `40a17f02`).
- Method: read-only. Everything below via `git show` / `git diff` from `C:/Users/david/projects/aa-claude-hotfix`. No builds, no browsers, no GPU, no npm. `aa-claude-hitl` untouched.
- AGENTS.md read in full; its standing rules applied (no weakened gates, claim-states separated, entry-0-is-not-success).

## Verdict (candidate 8 as an owner HITL build): FIT-WITH-NOTES

Fit to play-test at `:4300`. Not publishable (see PUBLISH BLOCKERS — unchanged from REPORT §7 plus two provenance corrections below). The REPORT is honest about every red row and open item I could re-derive; my findings are corrections and tightenings, not contradictions.

## (1) Merge-set table

Format check passes: every taken lane carries either a claimed verifier verdict or an explicit pending label (`round 2 applied, re-verification pending`; killstreak `fix round applied, re-verification pending`; frame-hitches / bot-anim `verifier: SHIP-WITH-FIXES, item not fully proven`). Left-out reasons check out except F1:

- F1 — `REPORT.md` §1 row 8: "No `fp-*` branch exists on origin" is FALSE. `origin/contrib/dave-gaming-pc/claude/fp-taa-resolve` exists (`git branch -r | grep fp-`). Smallest fix: reword row 8 to "no `fp-*` lane carried a recorded SHIP verdict at merge-set close" (if that half is true — I did not find any SHIP verdict for it either) and stop claiming non-existence.
- F2 — `REPORT.md` §1 header: verdicts are said to come from "the ledger's **'HF-509 lanes - results'** table in `docs/PASS84_OWNER_FEEDBACK_2026-09-02.md`", but that file on this branch contains no `HF-509` string at all (case-sensitive grep: zero hits), and the four merge commits themselves say "verifier pending - merged on workflow evidence per orchestrator direction" (`156b9da3`, `ce46cd14`, `2d6d7a3a`, `f5c11ee8`). So the SHIP / SHIP-WITH-FIXES / DO-NOT-SHIP labels for the v7 lanes (including `mp-weapon-pickup` DO-NOT-SHIP in the excluded list) are untraceable from the cited source. They may be real verdicts recorded elsewhere (orchestrator queue, another branch); the REPORT just doesn't point there. Smallest fix: replace the ledger citation with links/SHAs to the actual verdict records, or mark those rows "verdict post-hoc, pending citation". Downgrades nothing, but the provenance must exist before PASS 95 cites these verdicts as release evidence.
- Verified real: `sh-l2-irradiance-volume` exclusion — its bake fix `aaade3a4` ("move SH-L2 bake off the cold transition") exists and post-dates the merge window, so "no verification + changes the exact cold path" is a true reason; candidate-9 routing stands. Nuketown2 accuracy/interiors arriving via `cold-path-2` reapply commits (`8a70a3b6` seen in log) is consistent with the YES-via-#2 rows. Revert/re-revert pair `58f71798`/`fcf77f6e` is as described and is the correct way to re-merge after a revert.

## (2) Integrator repairs — nothing relaxed

- Thin-metal verge bodies (`8ce2482d`, `src/nuketown2-arena.ts`): CONFIRMED correct. The four removed rows (speed-limit signs, street-name blades) match classes the fidelity gate records as deleted (`src/nuketown2-fidelity.test.ts` "keeps the front verge open" — `street name blade`, `speed limit` in the gone-list; furniture ceiling tightened 43→36). The registry's missing-surface throw is untouched (delete rows, not the fence), `hitsToOpen`/`holeRadiusM` on the two surviving sign-board panels unchanged, and the survivors are still built (`src/nuketown2-arena.ts:3370` pairs `verge sign board`). This is reconcile-with-the-cull, not weakening.
- Care-package corrections (same commit, `docs/evidence/pass95/care-package-grant-once/REPORT.md`): CONFIRMED honest. Host-authoritative row downgraded `[VERIFIED]`→`[OPEN]` with the exact mechanism (`src/legacy-main.ts:24350` guest `requestKillstreakActivation` returns non-null pre-admission, no activation-rejected message, rollback unreachable); line count corrected 37,396→37,395 in the safe direction. Issue (a) left OPEN with correct reasoning: revision-keying is exactly as blind as the ordinal when the ambiguous snapshot is identical in shape AND content — host-side package identity is the real fix, correctly deferred to candidate 9 as past the "small and certain" bar.
- House-destruction scan re-point (`81cc7f6c`, `src/house-destruction-live-integration.test.ts`): STRONGER, confirmed by reading. Router fallthrough is correct (`src/thin-metal-perforation-runtime.ts:126-150`: thin-metal first, then `houseFragment||houseMajorDebris`→`applyHouseBulletImpact`, else `applyBulletImpact`), `src/legacy-main.ts:4730` calls the hoisted router, and the new assertion requires that call — dead code in an uncalled module can no longer satisfy the composition. Every original assertion unchanged.
- Gun-range scan re-point (same commit, `src/gun-range-rack-presentation.test.ts`): STRONGER, confirmed by reading. `src/legacy-main.ts:30035` is the gated pre-await call (`if (selectedArena.id !== 'gun-range') batchSelectedArenaPresentation()`), `:30166` the ungated post-await call. The old bare-`indexOf` form could not see the exemption; the new form requires EVERY pre-await batching call to carry it and still requires an ungated call before `submitForegroundWebGpuFrame()`. Deleting cold-path-2's gate reds the new test. Correct.
- Killstreak restore + anti-drift pin (§3e): history reads as described. One note, not a finding: the added test pins `KILLSTREAK_DISPLAY_LABELS` equal to `field-support`'s `GAMEPAD_SUPPORT_LABELS` — a cross-lane coupling where a legitimate gamepad-label change breaks the killstreak gate. If it ever fires, pin each list to its own ratchet budget instead of pinning the two lists to each other.

## (3) Graph-TOPOLOGY removal (`0c24f6e9`) — sibling IS equivalent, with two documented narrowings

Verified by reading the sibling test (`src/nuketown2-pipeline-budget.test.ts`, `keeps every variant pair separated by its own selector uniform`, on the candidate branch):

- Same `MUST_DIFFER` table as the removed test. Flattening every variant's uniform to one value fails it: for every pair the non-colour `differing` set becomes `[]` and `expect(structural,…).not.toEqual([])` reds. The mutation-proof cited in the comment (zeroing `paintedPanelled` / `concreteVariant`) is the same mechanism. The <=8 upper bound, <=40-graph ceiling and vehicle-forge budget are untouched (separate tests, still present).
- Narrowing (a), deliberate and disclosed: colour-only separation no longer counts (`baseColor`/`sidingWainscotColor` filtered out). Under the uber-shader architecture that is the intended budget win, and the owner-veto path is correctly stated (fix = restore graph shapes, not the test). No objection.
- Narrowing (b), worth one sentence in the REPORT: the sibling pins that *some* non-colour uniform differs per pair, not *which* selector — a swapped-uniform bug (kerb driven by `lawnVariant` with distinct values) would still pass, provided each family shader still samples its own uniform (pinned separately for the four families). If a future pass wants the tighter property, assert the expected selector name per pair. Not a HITL or publish blocker.

## (4) Cold number and soak table — claim-states honest; debt rows identified

- Cold 21,807.6 ms (the task's "21,808" is this number rounded): CONFIRMED in `docs/evidence/pass94/candidate8/cold-admission.txt` (`cold transition 21807.6ms exceeded 10000ms`). Improvement arithmetic checks (24,065.5 − 21,807.6 = 2,257.9 ms, 9.4%). Budget preserved at 10 s, run correctly RED, degraded foreground admission (`drained: false`, 82/82 resets) and the F3 art-signal gap both reported OPEN rather than massaged. The "+2 s or revert" keep-decision for the accuracy/interiors lanes follows from the measured improvement. Debt: trial 1 threw, so no multi-trial table exists — re-run once Nuke Town has a real art-ready signal (F3).
- Soak table (§8): HONEST, re-derived from `docs/evidence/pass94/candidate8/mp-soak.txt` — `divergences 100`, `seenByEveryoneAfter true` + `damageLatencyMs null`, stair `false/false`, 5 PASS / 3 FAIL, "no bound loosened". Re-verification debt, in order: MP-SOAK-REJOIN-DAMAGE (owner complaint fixed, latency half unproven — instrument gap: record the latency), MP-SOAK-REPLICATION (606→100 is real progress, still red), MP-SOAK-STAIR-FIRE (unchanged red). The REPORT's call that weapon-swap replication (32 swap findings + mp-audit's 16 swap / 12 reload-visibility / 4 relay-gap, all directions) is the biggest live MP defect and the next lane is endorsed — it maps onto HF-504's "cannot reload or pick up guns", and the relay-gap shape (host receives a guest message type it never relays) suggests one missing relay path, as the REPORT says. mp-audit killstreak detail corroborates item #6 half-working: banners/announce/phase replicate within 0.7 m on both peers, but guestB damage-source is `null` — the owner's headline requirement ("clear source when shot by the chopper") needs that one field before the lane closes.

## (5) Harness defects — both confirmed from the scripts

- H1 — `qa:stock-boot` cannot start its own server here. `playwright.config.ts:125,133` runs `node scripts/qa/playwright-web-server.mjs` under a 180,000 ms `webServer` budget, but that script does `await build()` (`playwright-web-server.mjs:7`), then stages the full release topology (`:12`, measured 5 m 45 s per REPORT), then `preview()` (`:16`) — so the budget dies before Chrome opens. Harness/machine problem, not a product regression; not widening the timeout was right (widening masks the next slowdown). Smallest fix: move staging out of the webServer lifecycle — an explicit pre-step (or a skip-if-already-staged flag), so the owned-server path only builds+serves inside its budget; keep `QA_EXTERNAL_PREVIEW=1` against an out-of-band preview as the documented fallback, exactly as the REPORT ran it (4/4 green).
- H2 — staged root has no `[data-release-choice="latest"]`. `scripts/release/stage-release-topology.mjs:429` writes the root as `kind: 'chooser-only'`; `release-shell/release-shell.js:168` keys every card by channel (`button.dataset.releaseChoice = key`, i.e. `experimental`/`previous`/`retained`/…) — there is no `latest` key in that vocabulary. `latest` belongs to the app's own bootstrap chooser (`src/bootstrap.ts:107`), which the shell overwrites at the dist root. So the spec's `tests/e2e/pass93-stock-flags-boot.spec.ts:137` click only works against unstaged `dist/` (what `:4300` serves — hence 4/4). Smallest fix: in the spec, fall back to `[data-release-choice="experimental"]` when `latest` is absent (staged root), or pin the gate to unstaged dist and record that the staged shell is a different front door — the REPORT's "two different front doors" warning stands and should be a PASS 95 publish-path checklist item, not just prose.

## Owner test order / decisions (no changes proposed)

REPORT §§5–6 stand: care-package mash, chopper damage numbers, killstreak-in-solo plus damage direction + audio-stop, soak items, prone-cap-proven vs leg-tangle-unproven (look at crouched/prone bots directly), freezes NOT fixed (GPU/present side per candidate-7 measurement — `qa:hitches` must fix its four instrument defects before its numbers are trusted), visuals via accuracy/interiors only. Chopper splash inversion (16 splash vs 13 direct) stays an owner call; one line + snapshot row if inverted.

## PUBLISH BLOCKERS (candidate 8 must not publish as PASS 95 until)

1. Cold-admission smoke green against the preserved 10,000 ms budget (measured 21,807.6 ms).
2. `qa:mp-soak` green on REPLICATION, REJOIN-DAMAGE (with non-null latency), STAIR-FIRE.
3. Open lane items closed: care-package queue-shape package identity, bot leg-tangle proof, frame-hitches real source fix (instrument first).
4. `v7-killstreak-awareness` fix round and `mp-soak-red` round 2 re-verified at their merged heads (both merged pending).
5. H1/H2 understood and fixed: gate owns its server lifecycle; staged shell vs app chooser front-door gap closed or pinned.
6. F3 (Nuke Town cold art signal) and F5 (graphics hashes re-pinned unmeasured) carried OPENs re-measured.
7. F2 provenance: v7 verdict labels cite their actual records (or are marked pending-citation) — release evidence must trace.
