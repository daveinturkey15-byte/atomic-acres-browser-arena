# Handoff 2026-09-05 09:30 — Claude Code (Fable 5.1) → Codex / Astra

Owner directive (HF-519/520): Claude Code orchestrated until ~09:30; Codex with Astra takes over orchestration and evaluation. This file lists what is live, what is in flight, what is paused, what is remaining, and the rules that must not change. The running log is the ledger `docs/PASS84_OWNER_FEEDBACK_2026-09-02.md` on branch `contrib/dave-gaming-pc/omp/pass84-overnight` (every owner request is an HF row; read HF-487 → HF-520 first). Times in this repo's ledger written by Claude between 06:00 and 09:00 drift up to +20 min; trust git commit times and file mtimes.

## 1. Live and served

| Surface | State | Evidence |
|---|---|---|
| GitHub Pages (live channel) | **PASS 93** (published 2026-09-04 08:08, `gh-pages` @ `7c9f1033`; PASS 92 pinned as the single safe rollback per HF-400). Nothing published since. | ledger PASS 93 row |
| `http://127.0.0.1:4300/` (owner HITL port) | **Candidate 8, served 09:33** (runtime `4b5cc28b`, branch `pass93-candidate` pushed at `32d8dcb0`, vite preview pid 189676, bundle SHA verified). Gates: tsc 0, coplanar 0/0, full vitest 631 files / 6,359 tests / 0 failures, build 0, stock-boot 4/4, bot probe ok, ratchet 37,391/37,396, nothing weakened. Cold transition 21,808 ms (candidate 7: 24,066) vs 10,000 budget. Soak 5/8: replication FAIL (606 → 100 divergences), rejoin damage FAIL only on latency null (seenByEveryoneAfter now TRUE), stair fire FAIL. Taken: gate-audit fixes, cold-path-2 (+ accuracy-3 and interiors readmitted at zero cold cost), soak-red + round 2, thin-metal, care-package, chopper, frame-hitches, bot-anim, killstreak fix round. Left out: visual-polish (DNS), sh-l2, albedo, weapon-pickup, taa. Correction: rooflines and turning-head were already in candidate 7 via geometry-2 (the critic's flat-roof finding is a design gap, not a missing merge). Not run: qa:hitches, captures, blind A/B. **Muse third-eye review: FIT-WITH-NOTES** as a HITL build, not publishable; two REPORT provenance corrections (an `fp-taa-resolve` branch does exist; the HF-509 verdict table lives on the ledger branch, not the candidate branch). | `C:/Users/david/projects/aa-claude-hitl/docs/evidence/pass94/candidate8/REPORT.md`; ledger `## Candidate 8`; `docs/evidence/pass95/candidate8-muse-review/REVIEW.md` |
| Cockpit (Foundry OS) `:47821` | live on the pre-delivery head; delivery branch `feat/control-plane-delivery-2026-09-04` @ `66522ee` in `C:/Users/david/projects/worktrees/foundry-fleet-contract-poc-integ` waits for the owner's "go" (Fleet.cmd restart, ~30 s) | ledger HF-506/507 |
| Provider usage poller | scheduled task `Foundry usage web poll` every 20 min; needs the owner's one-time login per provider: `python scripts/usage_web_poller.py login <provider>` from the cockpit delivery checkout | ledger HF-507 |

**PUBLISH STATUS (11:37): candidate 8 is being published as PASS 94, not 95** — the cut found PASS 94 was never published (gh-pages has pass92 + pass93 only; `publish_pass95` had nothing to pin), while `publish_pass94.py --dry-run` from the same byte-identical build passes every guard (live pass94, pinned backup pass93, pass92 retired). Publish agent (Opus) running with `docs/pass84-lanes/PASS94-PUBLISH-BRIEF.md`; receipt lands in `docs/evidence/pass95/publish/RECEIPT.md` on `release/pass95` and a `## PASS 94 publish record` section in the ledger. The next pass number is 95. Earlier plan for reference: owner verdict on candidate 8 is GOOD, publish requested (HF-522). Cut started on Opus from `32d8dcb0` in the release checkout `aa-claude-release84` (ritual to `--dry-run`, then an adversarial verifier of the cut evidence; publish only after the owner's explicit word on the two red publish gates below, recorded as an owner override). **Owner override GIVEN 10:22 (HF-523): publish PASS 95 with the cold-admission smoke (21.8 s) and qa:mp-soak (5/8) red; gates unchanged; follow-up lanes own the reds.** Publish step: Opus agent with the brief `scratchpad/opus-pass95-publish-brief.md` (copy in `docs/pass84-lanes/PASS95-PUBLISH-BRIEF.md`), launched when the cut workflow returns READY + PUBLISHABLE-WITH-OWNER-OVERRIDE; it writes `docs/evidence/pass95/publish/RECEIPT.md` on `release/pass95` and a `## PASS 95 publish record` section in the ledger. Watch the ledger `## PASS 95` sections.

**Publish rule:** no PASS 95 publish without the owner's HITL verdict, a green cold-admission smoke (preserved 10,000 ms budgets) and a green `npm run qa:mp-soak` (required gate since PASS 95). Cut ritual: `scripts/roll_pass.py --pass N`, tsc, release tests, publish-plan test, full vitest, build LAST, identity grep, `dist-passN` copy, `qa:release-identity`, freshness guard, boot smoke, `qa:stock-boot` with `PASS73_NATIVE_WEBGPU=1`, `publish_passN.py --dry-run` then publish, live checks; HF-400 backup-channel policy.

## 2. Candidate 7 → candidate 8 (what the owner said, what was done)

Owner verdict on candidate 7 (HF-509, 06:50): care package re-grants on every press; still some freezing; halve chopper gunner damage; visuals/gameplay better but not using the provided graphics/asset skills; bot prone/crouch legs tangle, max two prone; killstreaks must be visible/audible to every peer with proximity audio; minimap too cluttered on every map (HF-510). Independent gate audit (Opus): no threshold moved across 651 commits; one deleted test (graph-topology variants) restored and found red for a documented reason (see decision D1 below); three smaller items fixed on `v7-gate-audit-fixes`.

Verdict lanes (Opus/Fable builders, Opus verifiers; results table in ledger `## HF-509 lanes - results`):

| Lane | Branch @ head | Verifier | State / next |
|---|---|---|---|
| care package grant-once | `claude/v7-care-package-grant-once` @ `866de9ef` | SHIP-WITH-FIXES, owner item fixed | in candidate 8 merge set; fix to apply: revision-keyed package-instance id (`src/care-package-grant-once.ts:186-204`); guest grant is optimistic (host→client reject message is a follow-up) |
| chopper gunner damage | `claude/v7-chopper-gunner-damage` @ `bc57baf6` | SHIP | in merge set; 25.5→12.75 per hit; owner decision D2 on splash (16) vs direct (13) inversion |
| killstreak awareness | `claude/v7-killstreak-awareness` @ `63bc7020` | SHIP-WITH-FIXES, 3 confirmed regressions → **fixed by Luna 08:57** (548ab778 damage direction, 6b71961b match-end flight audio, 0c567668 solo announcements; 614 tests green) | integrator told to merge the fixed head into candidate 8; Muse review 2 queued; Opus re-verification after the reset |
| frame hitches | `claude/v7-frame-hitches` @ `ce1305c6` | SHIP-WITH-FIXES but no source fix | attributor script exists (`qa:hitches`), measured p99 40.3 ms / p99.9 65.5 ms with 4 bots; four instrument defects to fix, then hunt causes. NOT in candidate 8 |
| bot prone/crouch animation | `claude/v7-bot-anim-prone-crouch` @ `988cfd39` | SHIP-WITH-FIXES, prone cap yes, leg tangle unproven | needs a real capture pair proving the leg fix; NOT in candidate 8 |
| visual polish from skills | `claude/v7-visual-polish-from-skills` @ `85f7e066` | DO-NOT-SHIP | "after" captures came from the base bundle; re-capture at `85f7e066` and re-judge |
| gate-audit fixes | `claude/v7-gate-audit-fixes` @ `235432d5` | — | in merge set (F1 red by design, F3, F4, F6) |

Luna lanes finished this morning: `claude/cold-path-2` @ `30f92d2a` (ROOT CAUSE of the shared fatal `THREE.AttributeNode position not found`: the waterless "Pass 64 TSL perimeter water" placeholder used an empty BufferGeometry → PlaneGeometry; static batching hoisted; async cold precompile; cold transition still 23.7 s vs 10 s, measured under 100 % CPU) and `claude/mp-soak-red` @ `bd10468c` (divergences 606→147 in a partial run, classified: stale-snapshot-never-applied 80 / guest-self-prediction 65 / ordering; rejoin identity preserved; soak NOT certified — needs a quiet-machine `npm run qa:mp-soak`).

Gemini reference critic on candidate 7: **76/100** (63 → 76). Top three: authentic rooflines (+12; NOTE the candidate 8 integrator found `nuketown2-rooflines` and `nuketown2-turning-head` were already in candidate 7 via `nuketown2-geometry-2`, so the flat-roof finding is a design gap in the merged roof work — butterfly prow with solar panels south, capsule roofs north — not a missing merge), vehicle liveries (+8; done by Muse on `muse/nuke-liveries-flagstones` @ `5f2986fe`), flagstone path (+5; same Muse branch). Review: `aa-claude-research` `docs/evidence/pass94/gemini-reference-critic/candidate7-REVIEW.md`.

## 3. In flight right now (OS processes; they outlive the Claude session)

| Job | Harness | Where | Done marker |
|---|---|---|---|
| Candidate 8 integrator | Claude Opus agent | worktree `aa-claude-hitl`, branch `pass93-candidate`; brief `scratchpad/opus-candidate8-integrator-brief.md` | ledger section `## Candidate 8`, REPORT in `docs/evidence/pass94/candidate8/`; **DONE: served 09:33** (see §1). Its REPORT lists the top new defect (weapon swap + reload not replicated: 32 SWAP-NOT-REPLICATED in the soak, 4 RELAY-GAP in the audit; guestB's killstreak damage-source label null; rejoin row fails only on damageLatencyMs null) — the **Luna lane `mp-swap-reload-relay` is DONE 10:38 @ `a9b4b029`** (host-authoritative broadcast of canonical swap/reload/damage state, observer ammo projection, victim label on non-controller peers, rejoin latency sampling; 683 tests green; browser audit [OPEN]; Muse review queued). Not in PASS 95; first candidate for PASS 96 after re-verification and a quiet-machine soak. Muse third-eye review of candidate 8 queued (job 472 → `docs/evidence/pass95/candidate8-muse-review/REVIEW.md` on the ledger branch). Publish blockers per the integrator: cold fence (21.8 s), three soak rows, re-verification of killstreak + soak round 2, care-package queue-shape id, and the qa:stock-boot harness defect |
| Killstreak fix round | Luna high | `aa-v-killstreak-awareness` | **DONE 08:57 @ `63bc7020`**; merged into candidate 8 as `c3ca1c72` |
| Farcrysis layout | Luna xhigh | `aa-m-farcrysis`, branch `claude/v9-farcrysis` | **DONE 11:23 @ `7197e427`** (194 tests, coplanar/parity/build green; 6/6 stations, 32 routes probed, 0 invisible walls; sightline band [OPEN] 391/1008; arena still hidden). Muse review queued (475). |
| Farcrysis dressing | Luna xhigh | `aa-m-farcrysis-dressing`, branch `claude/v9-farcrysis-dressing` (from the layout head) | `CODEX-FARCRYSIS-DRESSING-DONE` in `scratchpad/codex-luna-farcrysis-dressing.log` (launched 11:25, 120 min box, port 4268) |
| Soak round 2 | Luna xhigh | `aa-claude-soakred`, branch `claude/mp-soak-red` | **DONE 09:12 @ `8ff4d236`**: three root causes fixed with unit tests (guest apply fence was sequence-only → continuity first; self-id path never reconciled the predicted pose → reconciles beyond 0.35 m; host never sent the fresh rejoiner state slot to observers → replayed, credit keyed by playerId:connectionEpoch). 365 tests green. Certifying `qa:mp-soak` still [OPEN] (quiet machine). Integrator told to merge before its soak; Muse review queued |
| Nuke Town art-ready cold signal | Luna high | `aa-claude-artready`, branch `claude/nuketown2-art-ready-signal` | **DONE 08:53 @ `70afd55c`** (generic per-arena art contract, nuketown2 integrated, smoke assertions consume it; unit gates green; browser smoke [OPEN]; Muse review queued). Based on `v7-gate-audit-fixes`; mergeable into candidate 9 after one cold smoke |
| TAA forward-port | Luna high | `aa-claude-fptaa`, branch `claude/fp-taa-resolve` | **DONE 08:59 @ `f1eabb57`** (fingerprints regenerated by script, capability expectations changed only where TAA justifies, full suite 6,250 tests green, build green; browser perf at QUALITY [OPEN]; Muse review queued). Mergeable into candidate 9 after the perf measurement |
| Muse Spark 1.3 reviewer pool (6 workers) | OMP + Muse | queue `scratchpad/muse-queue/{pending,running,done,logs}`; drop a `.md` prompt in `pending/`; a `STOP` file makes workers exit | job prints `MUSE-TASK-DONE` |
| Muse builder pool (6 workers, 90 min max) | OMP + Muse | queue `scratchpad/muse-queue-build/…`; running/queued: 373 Nuke interior furnishing, 374 backdrop ridges + window depth + vehicle roofs, 375 finish audio-polish (2D shed door, wire positional APIs), 376 finish time-of-day (match-time + arena-default wiring), 377 finish hud-menu (1280x720 overflow, ramp rule, per-frame HUD writes) — each pushes to its lane branch | `MUSE-TASK-DONE` |
| Gemini mechanical verification of the 7 polish lanes | OMP + Gemini 3.8 Flash high (`google-antigravity/...` route) | `scratchpad/gem-verify/run-all.cmd` (sequential); writes `docs/evidence/pass95/<lane>/GEM-VERIFY.md` on each `v8-*` branch | **FAILED 08:45: all seven exited within seconds ("Deadline exceeded" / empty) — the OMP Gemini route goes through the same Antigravity account whose 5-hour "Individual quota" is exhausted until ~11:55. Re-run `run-all.cmd` after 11:55 (prompts and launchers are intact)** |
| OMP credential guard | loop | restores `~/.omp/agent/agent.db` `auth_credentials` from the newest `agent.db.bak-*` every 2 min. **The store is wiped about every 30 min today (09:14, 09:45, 10:15); root cause unknown.** A Muse job that starts inside the wipe window fails instantly (log of ~11 bytes, `DONE-MARKER no`): requeue it by copying the job file from `done/` back to `pending/` under a new number. Check rows with `python -c "import sqlite3;print(sqlite3.connect(r'C:/Users/david/.omp/agent/agent.db').execute('select count(*) from auth_credentials').fetchone())"` | `scratchpad/omp-cred-guard.log` |

`scratchpad` = `C:/Users/david/AppData/Local/Temp/claude/C--Users-david-Desktop-stuff/4cfe1b40-a256-4a42-8146-934f0cf22570/scratchpad` (session-specific; copy anything you need to keep).

## 4. Paused (Claude workflows — NOT resumable from Codex; relaunch as lanes)

The Claude usage budget (25 % left, resets ~12:25) and machine memory forced three workflows to pause. Their lanes pushed per step, so the branches carry the work; each has a Muse review at its head (the review file names the UNFINISHED items).

| Wave | Lanes (branch @ head) | State |
|---|---|---|
| Polish (HF-511) | `v8-minimap-simplify` @ `29c80087`, `v8-hud-menu-polish` @ `c0b13857`, `v8-weapon-feel` @ `de92bfc8`, `v8-bot-behaviour` @ `29ca371e`, `v8-draw-call-instancing-audit` @ `dd1fd413`, `v8-audio-polish` @ `7c794e88`, `v8-time-of-day-weather-lighting` @ `63e254ef` | builders paused mid-lane; Muse reviews done (`docs/evidence/pass94/muse-review/v8-*-REVIEW.md` on each branch): minimap SHIP-WITH-FIXES (live HUD clip harness added, roster proof pending), hud-menu SWF (HUD 2.85 ms/frame vs 1.5 budget still open; menu overflow at 1280x720), weapon-feel **DO-NOT-SHIP** (recoil tables done, no capture, gates cited not run), bot-behaviour SWF (stuck ladder + fairness gate done with live evidence), draw-call audit SWF (browser draw-call confirmation unrun, one arena measured), audio SWF (one shed-door path still 2D at `legacy-main.ts:15354`; new positional APIs unused), time-of-day SWF (match-time and arena-default preset wiring unwired). Gemini mechanical verification running. Next: finish each UNFINISHED list (Luna/Muse), Opus verify after the reset, candidate 9 |
| Farcrysis + Raid (HF-512) | `v9-farcrysis` @ `de910d2f` (SPEC.md + layout stage part-done by Fable), `v9-raid` @ `85aa65ea` (FINDINGS.md research done) | **Farcrysis layout lane LAUNCHED 09:14 on Luna xhigh** (worktree `aa-m-farcrysis`, marker `CODEX-FARCRYSIS-LAYOUT-DONE` in `scratchpad/codex-luna-farcrysis-layout.log`, 120 min box). Raid layout prompt ready: `scratchpad/codex-luna-raid-layout-prompt.md` (worktree `aa-m-raid` needs `npm ci`; launch with `codex-luna-raid-layout.cmd` made from the `codex-luna-coldpath2.cmd` template once candidate 8 is served). Dressing stage (assets/textures/vegetation) after layout |
| Forward-ports of reviewed lanes | 3 of 13 started: `aa-fp-lobby-countdown`, `aa-fp-nuketown2-interior-look` @ `83186bc0`, `aa-fp-transmission-glass-windows` @ `30897812` (nothing pushed yet). Remaining: mp-lobby-overhaul `8d8d1ef7`, skyline-ground-projected-env `f24a2565`, volume-fire-emitters `dc2a3e7f`, ssr-temporal-denoise `90da85f0`, taa-resolve `1f847b4b` (DONE as `fp-taa-resolve` @ `f1eabb57`, see §3), raid2-slice-2 `1c62b74f`, farcrysis-slice-2 `eabb24c0`, raid2-generator-building-detail `0226edfa`, nuketown2-rooflines `a01c3494`, nuketown2-turning-head `0e393367` | each: new worktree from `pass93-candidate`, merge lane, resolve toward the candidate's authority controls, gates, ONE cold smoke under the lock, push `fp-<lane>` |

Other ready branches: `claude/sh-l2-irradiance-volume` @ `aaade3a4` (Muse moved the 2.45 s bake off the cold transition with a digest cache — needs a browser cold smoke to confirm, then mergeable), `claude/all-arenas-air-and-coplanar` @ `ea28dc89` (SWF fixes applied), `claude/thin-metal-perforation` @ `f42fbb70` (Muse SHIP), `claude/nuketown2-accuracy-3` @ `3a18728a` and `claude/nuketown2-interiors-accuracy` @ `e4ba6832` (unblocked by cold-path-2), `claude/c7-gate-audit` @ `91b7afa3` (audit report), `muse/nuke-liveries-flagstones` @ `5f2986fe`, `claude/materials-albedo-variation` @ `5db92f9e` (owner judgement on the 61/255 delta pending).

## 5. Remaining owner asks not yet covered by a lane

- HF-509 #2/#4 "freezing / lagging and chopping": attributor exists, no cause fixed yet (frame-hitches lane) — highest value next lane; run on a quiet machine.
- HF-509 #5 bot leg tangle proof; HF-509 #4 "use the provided graphics/asset skills" (visual-polish lane rejected; the skill store is `C:/Users/david/.claude/skills`, technique register `%LOCALAPPDATA%/hermes/.akephalos/references/ai-3d-technique-register.md`).
- HF-510 minimap simplification on EVERY arena (v8-minimap-simplify part-done).
- HF-499/504 multiplayer: soak certification (replication, rejoin damage, stair fire), guest reject message for care-package/killstreak activation, lobby host-migration browser e2e (OPEN), WAN evidence with friends.
- Cold admission: 23.7 s vs 10 s (visual-definition GPU phase) — needs a quiet-GPU lane; art-ready signal lane running.
- HF-512 Farcrysis rebuild + Raid accuracy (paused, see §4). HF-495 map order done. Full backlog: `docs/pass84-lanes/BACKLOG-2026-09-04.md`.

## 6. Decisions waiting on the owner

- **D1** the deleted graph-topology test: restored it fails 7/8 pairs because `af1fce7d` moved variants into selector uniforms (detail preserved; a mutation-proven sibling test enforces the same property). Candidate 8 keeps the sibling and drops the old test in its own commit. Owner may veto.
- **D2** chopper gunner: after halving, splash (16) now exceeds direct hit (13) — keep or halve splash too.
- **D3** albedo variation lane: capture delta 61/255 — accept or reject.
- **D4** cockpit "go"; provider logins for the usage poller.

## 7. Rules and machine facts (do not relax)

- Never weaken, skip or widen any test, threshold, fence (12 s WebGPU), budget (10 s cold, pipeline 54, in-combat tripwire 0), soak bound, coplanar class (0), parity (0), fidelity band, or the legacy-main size ratchet (LINE_CEILING 37,396: hoist, never raise). Regenerate fingerprints with their scripts; never hand-edit.
- Headless browsers only (installed Chrome, `PASS73_NATIVE_WEBGPU=1`, stock flags, `--mute-audio`, off-screen; never on the owner's screen). One heavy step at a time through the machine lock `C:/Users/david/AppData/Local/Temp/aa-heavy.lock` (mkdir to take, rmdir to release; stale after 35 min). Free RAM below 2 GB starves `:4300`; 4 GB free before launching a browser lane.
- Never kill the owner's processes; cockpit `:47821` and `dsh :3080` stay. ComfyUI was closed by the owner at 08:2x (HF-514).
- Local Qwen retired (HF-492). Antigravity Opus 4.6 quota exhausted until ~11:55 (drops mid-run; give it small incremental briefs; no `--effort` flag for Claude models). Muse Spark 1.3 contributor and Gemini 3.8 Flash high are cheap and under-used: use them heavily (Muse builds and reviews; Gemini mechanical verification and image critique, never research). Luna for hard lanes.
- OMP one-shot: `omp -p "Read <prompt file> and do exactly what it says" --model google-antigravity/gemini-3.8-flash-high --thinking high --no-skills --no-lsp --no-session --allow-home --max-time 40`. Luna: `codex exec --model gpt-5.6-luna -c model_reasoning_effort=<high|xhigh> -c notify=[] --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox -C C:\Users\david\projects --add-dir C:\Users\david\projects -o <last.md> - < <prompt.md> >> <log> 2>&1` (see any `scratchpad/codex-luna-*.cmd`). Lanes: own worktree from `origin/contrib/dave-gaming-pc/claude/pass93-candidate`, push per step with `git -c credential.helper= -c "credential.helper=!gh auth git-credential" push`, REPORT with claim-states `[VERIFIED]/[MEASURED]/[OPEN]`, adversarial verification before merge, Muse review as the third eye.
- Skills: `~/.claude/skills` is a link into the shared flat view; if it reads empty, run `C:/Users/david/Documents/desky-bootstrap-clone/_Scripts/link_skills.ps1` (never copy). Log every owner request as an HF row in the ledger BEFORE acting.
- Scheduled tasks: `Foundry usage web poll` (20 min), `Foundry frontier scan` (07:00 daily). `AA candidate7 0500` and `AA gemini candidate7 0645` are one-shots already fired (delete or re-arm as needed).
