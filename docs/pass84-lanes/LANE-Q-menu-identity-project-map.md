# Lane Q — HF-406: top-right badge, features panel and project-map button show the CURRENT pass

Orchestrator: Claude Code (Fable 5.1). Ledger row HF-406.

Worktree: `C:\Users\david\projects\aa-claude-release84` (Lane F's, F is finished)
Branch: `contrib/dave-gaming-pc/claude/pass84-release-prep` (continue on it;
head 0259bfaa; Lane F's report and verdict are in `artifacts/lane-report.md`
and `artifacts/lane-verdict.md` there - read both first).

## Owner statement (verbatim, 2026-09-02 ~12:10 BST)
"ensure the top right thing is an accurate update of both the current pass
number and features, and the map button contains the proper project map
too. Currently it says pass 73 HITL?!"

## Facts
- Live site today: PASS 83 default (channels/pass83), chooser with older
  channels. The owner saw "PASS 73 HITL" somewhere top-right, so some
  identity surface reads a stale source (a hardcoded string, an old
  changelog entry, the HITL checklist title, or a cached shell file).
- Identity source: `src/release-identity.ts` (Lane F stamped PASS 84 /
  channels/pass84); changelog `src/changelog*.ts`; project map is pinned by
  `src/project-map.test.ts`; the release shell/chooser is
  `release-shell*.js` on gh-pages generated by the publish script; the HITL
  checklist is `docs/PASS74_HITL_CHECKLIST.md` (regenerated from the owner
  ledger) and may be surfaced in the menu.
- Known regression class: a PASS 82 publish shipped still calling itself
  PASS 81 (third time). The badge check must read the identity CHUNK
  (`dist/assets/release-identity-*.js`), never index.html.
- Lane F's dry-run and publish contract tests exist
  (`scripts/orchestration/publish_pass84_plan.test.mjs`); do not weaken them.

## Job
1. FIND every place the menu renders a pass number, a feature list, "HITL",
   or the project map: grep the src for 'PASS ', 'HITL', 'project map',
   'projectMap', 'changelog', 'release-identity', and the menu shell
   components (`src/ui/*shell*`, `src/legacy-main.ts` menu region by grep
   only). Build once and capture the menu headless at 1600x900 including
   the top-right region and the opened map/project-map panel; save under
   `artifacts/qa/hf406/before-*.png`. Also fetch the LIVE
   `https://daveinturkey15-byte.github.io/atomic-acres-browser-arena/` and
   `/channels/pass83/` headless and capture the same surfaces, to see
   exactly what the owner sees and which file it comes from (network
   panel: which JS chunk contains the string).
2. FIX so every surface derives from ONE source: the top-right badge shows
   the current pass number and a short feature line for THIS pass (from
   the changelog's current entry, not a hardcoded string); the features
   panel lists the current pass's features first; the map button opens the
   real project map (the document `src/project-map.test.ts` pins, rendered
   procedurally, current arenas and routes, no stale pass label). Remove
   or relabel any "HITL" surface so it is clearly the owner checklist and
   never masquerades as the version badge.
3. GATE: a unit test that renders the identity surfaces and fails if any
   contains a pass number other than the current pass or the word HITL
   outside the checklist; plus a step in the publish runbook that greps the
   built identity chunk and the release shell for stale pass numbers.
4. Re-capture the same views; `npx tsc --noEmit`; focused tests
   (`src/release-topology.test.ts`, `src/project-map.test.ts`,
   `src/changelog.test.ts`, the new test, Lane F's contract test); commit
   with explicit paths.

## Boundaries
- You own: menu shell identity surfaces, changelog current-entry wiring,
  project-map rendering, `src/release-identity.ts`, the new test, the
  runbook step. `src/legacy-main.ts` only in the menu identity region with
  `// HF-406:` marks, LF preserved.
- Do NOT touch: gameplay, arenas, viewmodel, lobby/netcode, the publish
  script's guards (add a check, never remove one).
- Machine rules: headless only, one browser, one build, never kill
  processes, never the full vitest suite, 3 GB free VRAM before a launch.

## Report
Before/after captures of every surface (local build and live), the file
that produced "PASS 73 HITL", the single source now used, test names,
commits. Claim-state every line.
