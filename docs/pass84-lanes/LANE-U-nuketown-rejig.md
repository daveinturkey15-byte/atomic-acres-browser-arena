# Lane U — HF-407: Nuke Town total layout rejig to the Black Ops 2 Nuketown flow, code-authored

Orchestrator: Claude Code (Fable 5.1). Ledger row HF-407. THE OWNER'S HEADLINE
ASK for the next pass.

Worktree: `C:\Users\david\projects\aa-claude-nuketown`
Branch: `contrib/dave-gaming-pc/claude/nuketown-rejig` (base = PASS 84 head 75a4e508)

## Owner statement (verbatim, 2026-09-02 ~16:10 BST)
"I don't think it's very true in layout or style to the original nuketown map
from black ops 2. I would like to totally remake the layout and feel to be
much more similar in terms of size and how the buildings and vehicle are to
nuke town from black ops 2, the flow of the map and its cover and houses and
stuff are important. We can then make our own artstyle and gameplay
enhancements and totally modify it ... still keeping things like the 2x
damage, the rare gun spawn, the sheds, but the layout needs a total rejig and
the bus can probably be made with code instead of blender and be better? do
they even have a bus in nuketown and if not maybe we don't need it? just
mirror what it has and the way the closed/open vehicles work as cover, the
black ops 2 version"

## What exists (verified 2026-09-02)
- Nuke Town = arena id `atomic-acres` (legacy alias `nuketown`, route
  `nuke-town`), the ONLY arena with `authoring: 'import'`: layout authority in
  `src/map.ts` (`buildArena`, 1,110 lines, TypeScript collision boxes) plus a
  Blender bake `public/assets/original/models/atomic-acres-blender-arena.glb`
  (7.3 MB) generated by `scripts/blender/create-atomic-acres-blender-arena.py`
  from `scripts/blender/export-atomic-acres-arena-spec.ts`. Visual module
  `src/rendering/arenas/atomic-acres.ts`; lawn `src/nuketown-lawn-field.ts`;
  forest `src/nuketown-forest-surround.ts`; mountain ring; sheds with
  see-through and push physics (bus v6 likewise); the 2x-damage overdrive core
  sits on the bus roof; rare-gun spawn; authored large-cover anchors;
  `src/nuketown-fidelity.test.ts` pins the current bands (map crossed in ~10 s
  sprint, two houses facing across the road with garages, bus in the road
  shortening the longest sightline, 180-degree collider symmetry, fence
  escape-proof, footprint measured from colliders).
- The 2026-08-29 redesign (`docs/NUKETOWN_REDESIGN_2026-08-29.md`) rotated the
  flow to end-garden spawns and lengthened the street; the owner still reads
  the result as cramped and untrue. Read that document: its diagnosis method
  (measure the reference proportions, then the built map) is right; its
  execution kept too much of the old geometry.
- Every other arena is code-authored (three/webgpu NodeMaterial + TSL,
  procedural). Lane P registered Map 3 today: registry row, rosters derived
  from the registry, boot smoke, menu-preview capture, eye-clearance ledger
  entry, spawn table from `scripts/qa/solve-spawn-layouts.ts`, art-direction
  row above the distinctiveness floor, `authoring: 'code'`. Follow that exact
  trail; the integration notes for Map 3 are in the ledger's final sections.

## Decisions already made by the orchestrator (owner-approved direction)
1. Build the rebuild as a NEW arena beside the shipped one, so the main map is
   never broken mid-pass: id `nuketown2`, route `nuke-town-rebuild`,
   displayName `Nuke Town Rebuild`, selectorLabel `NUKE TOWN REBUILD · PREVIEW`,
   `authoring: 'code'`, `selectable: true`, `multiplayer: true`, fieldSupport
   and overdrive as the current Nuke Town (the owner will host it with
   friends). When the owner approves, a later lane swaps ids; not you.
2. Everything in code. No Blender, no GLB, no imported mesh/image/font/LUT.
   Vehicles, houses, garages, fences, sheds, props: procedural TSL.
3. Layout and flow mirror the reference; the ART is ours (that is the owner's
   "make our own artstyle" - do a clean, readable first-pass style now; the
   art-direction lane refines later).

## Job, in order
1. REFERENCE STUDY FIRST, written down before any geometry. Use WebSearch /
   WebFetch for published overhead layouts and descriptions of Nuketown 2025
   (Black Ops 2) and the original Nuketown; derive and record in
   `docs/NUKETOWN_REBUILD_2026-09-02.md`: overall playable size (length along
   the street, width), the two houses (footprint, two storeys, which corners,
   diagonal placement, garages attached or detached, driveway cars), the back
   yards and spawn areas (fences, shelter/hedge line, side paths), the street
   centre (what vehicle is there - the owner asks "do they even have a bus";
   answer it from the reference and record it: if the centrepiece is a moving
   truck and cars, say so and build that; if there is no bus, there is no bus),
   which vehicles are open (enterable / see-through) versus closed cover,
   kerb-side props, the three movement lanes and where cover breaks the
   sightlines, upper-floor window sightlines. Record proportions as ratios so
   the build can be scaled; the owner said "similar in terms of size", so the
   playable footprint should match the reference's proportions, then be tuned
   with the fidelity gate's sprint-time band. Copy NO asset, NO texture, NO
   text from any source; describe, measure, rebuild.
2. LAYOUT in code: `src/nuketown2-arena.ts` (pattern: `src/map3-arena.ts`,
   `src/test-maps.ts`) with TypeScript collision authority, colliders for
   every house floor, wall, stair, garage, vehicle, fence and shed; interiors
   enterable on both floors; windows as real openings; 180-degree rotational
   symmetry like the reference (the fidelity test's symmetry check is the
   right instrument). Spawn table from the solver in the two end yards.
3. GAMEPLAY features carried over, by code: the 2x-damage overdrive core on
   the roof of whatever the reference's central vehicle is; the rare-gun
   spawn; the sheds with see-through + push physics (reuse the existing
   module); open/closed vehicles as cover exactly as the reference treats
   them (open = you can be inside or see through; closed = solid).
4. VISUALS, first pass, procedural TSL: house shells with our own palette,
   garage doors, vehicles, fences, kerbs, props, lawn field + forest surround
   + mountain ring reused from the existing modules where they take a layout
   parameter. Register a visual module under `src/rendering/arenas/nuketown2.ts`
   with review cameras (overhead, each spawn yard, street centre, both upper
   rooms). Art-direction row that clears the distinctiveness floor
   (`src/rendering/art-direction.test.ts`; saturation/contrast do not enter
   the metric, gain/lift/gamma/crosstalk do).
5. REGISTER (Lane P's trail): registry row, `ARENA_IDS`, spawn-builder
   record, rosters, boot smoke green headless (PASS73_NATIVE_WEBGPU=1 is
   headless now), menu-preview capture, eye-clearance ledger entry, collider/
   visual parity audit, spawn-quality gate green, fidelity test for the
   rebuild (copy `src/nuketown-fidelity.test.ts` into `nuketown2-fidelity.test.ts`
   with bands re-derived from the recorded reference proportions and the
   reasons written next to each number).
6. EVIDENCE: overhead minimap render of the rebuild beside the recorded
   reference proportions (a diagram YOU draw from the measurements, never a
   copied image), the review-camera frames, a 60 s headless solo run with fps
   and zero page errors, and the honest list of what still differs.
7. `npx tsc --noEmit`; focused tests; commit per step with explicit paths.
   Timebox: this lane is the INITIAL REJIG - a playable, correctly-flowing
   preview with first-pass visuals. Do not spend the budget on art polish.

## Boundaries (hard)
- You own: `src/nuketown2*`, `src/rendering/arenas/nuketown2.ts`, the
  rebuild's registry/roster/spawn/art-direction rows (`// NUKETOWN2:` marks),
  the design doc, the new fidelity test. Reuse existing modules by import;
  if a module needs a layout parameter, add it without changing the shipped
  Nuke Town's behaviour (its tests must stay green).
- Do NOT touch the shipped Nuke Town (`src/map.ts`, the GLB, its Blender
  scripts, its fidelity test), weapons/viewmodel, lobby/netcode, other arenas.
- Machine rules: headless only (a guard kills headed browsers), one browser,
  one build, never kill processes, never the full vitest suite, 3 GB free
  VRAM before a launch, explicit-path commits, LF in legacy-main.

## Report
The reference measurements and the bus answer; the rebuild's measured
proportions against them per element; what carried over (core, rare gun,
sheds); gate results; frames; what still differs and what the art lane
should do next. Claim-state every line.

## ADDENDUM (orchestrator, 18:15 BST) — adopt the brief-with-embedded-rules protocol now
The owner sent a production brief whose method produced a photoreal scene in
one autonomous run (see `docs/pass84-lanes/LANE-AJ-brief-driven-scene-skill-map3.md`
for the resolved structure) and asked that its useful techniques be applied
to this rebuild "being usage conservative and not breaking or ruining
anything". Adopt, at zero extra browser cost:
1. Working documents in `docs/nuketown-rebuild/`: SPATIAL_PLAN.md (your
   measured reference proportions and the built layout side by side),
   ASSET_INVENTORY.md (every structure and prop with state and collider
   status), TASK_STATE.md (checkpoint so a restart resumes, not repeats).
2. A FIXED judgeset of review cameras (both spawn yards, street centre from
   both ends, both upper rooms, garages, central vehicle, overhead); commit
   them; every capture in your report comes from that set so the skeptic
   compares like with like.
3. The three-reading-distances rule for every substantial object:
   silhouette at range, structure at mid, joins up close; and the
   critical-failure list: floating or intersecting props, default
   materials, a zone that exists only as a sign, darkness or fog hiding
   unfinished work, reverse angles unfinished.
4. Your skeptic will score a rubric (layout fidelity to the recorded
   proportions 30, flow and cover rhythm 25, collision parity 15,
   readability in combat 15, technical hygiene 15; gates >= 85% each). Put
   the numbers you expect against each in your report.
The art and dressing pass by the full critic loop is Lane AK, after you.
