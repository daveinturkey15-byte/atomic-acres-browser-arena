# Atomic Acres overnight owner contract, HF-533/HF-534

Owner authorized overnight work 2026-09-05. End by 2026-09-06 08:00 Europe/London, or earlier usage/handoff stop. Main goal: fix observed multiplayer bugs and returned in-match freezing, set Nuke Town to two bots, simplify measured hotspots, then substantially improve Nuke Town visuals without losing frame rate. Once Nuke Town's blocking checks pass and visuals are materially improved, develop the bare Raid preview. No claim of zero future bugs; add regression coverage for demonstrated failure mechanisms.

Priority: (1) guest-to-guest replication, rejoin damage, reload after death; host/guest lobby and migration, care-package reject/killstreak follow-through; (2) exactly two AI bots on Nuke Town bot-enabled solo/host modes, never alter human slots or other-map counts; (3) capture and repair freezing/hitches and cold admission; (4) rich, coherent Nuke Town materials, lighting, trees, mountain terrain/ground, particles/effects and plausible physics; (5) Raid after Nuke readiness, other-map retained regressions and queued polish/forward-ports.

Budgets: root Codex begins at 39% weekly remaining. Check account usage at every heartbeat and before any Codex worker; root is orchestration/independent validation only. At <=22% prepare handoff, at <=20% stop new Codex implementation and attempt Claude Desktop transfer if quota and app permit; close out at <=17% to preserve the owner's requested 15% floor. No reset-credit redemption or purchases. Claude jobs require a fresh supported numeric weekly usage reading, at least 35% remaining before dispatch, short bounded calls, stop dispatch at <=30%; retain >=25%. Unknown/stale quota blocks Claude dispatch. Exact Claude Desktop handover at 20% is conditional on a working supported surface, adequate quota and acknowledgement/start evidence, not merely writing a file. Repair task id 01a07300-a6fb-7460-aa7c-a6172fda44d8; do not modify Claude's installation or launcher here.

Use meta-contributor/muse-spark-1.3 via OMP for most work; google-antigravity Gemini models are authorized when route/quota verified. Claude Opus/Fable permitted only inside the reserve. Never silently substitute a model or claim actual route/token cost from planned-route metadata. No local inference/ComfyUI launches. Maximum three concurrent external workers, one generation of workers (no nested delegation), 60 minutes per implementation job, 45 minutes per read-only critic, maximum 12 jobs this night, two attempts per unchanged failure mechanism before a different qualified critic or explicit OPEN handoff. Stop on first full pass; no repeated green suite runs. All CLI jobs go through the retained run-ledger wrapper.

Freeze codebase source at candidate base 0d8e1df8 (runtime acaf7284) for first wave. Each worktree has one owner; never write a sibling or the owner release/HITL checkout, :4300, main or gh-pages. Root integrates after independent review; no public publication this night without the owner's new HITL verdict and the live release contract. Main ancestry is inherited OPEN: do not reconstruct/reset the history or replay only PASS95 onto old main. Never modify tests, thresholds, ratchets or fences to obtain green. Owner-requested bot count is an intentional behavior change: update only its exact expectation with retained guards.

Read the assigned worktree AGENTS.md, docs/CONTRIBUTION_AND_RELEASE_PIPELINE.md, docs/MULTI_AGENT_REPO_DISCIPLINE.md, docs/evidence/pass95/astra-45m/REPORT.md. Shared skills are real files, not skill:// resources. Read C:/Users/david/.codex/skills/threejs-game-development/SKILL.md and the named lane skills by full filesystem path; these are links to the shared canonical vault. Do not privately copy or edit skills. On Three.js API work inspect installed r185 and current docs first: https://threejs.org/docs/llms.txt and llms-full.txt, then Poimandres and upstream versioned sources/examples. Skills never override source. Relevant shared skills include multiplayer, frame-loop audit, procedural art, vegetation, image-to-Three.js, visual-gauntlet-loop and asset generation; record exactly which were loaded.

Run contribution preflight before implementation and before handoff using --harness omp; its inherited main-ancestry failure stays OPEN and blocks publication, not the owner's authorized isolated local repairs. Commit explicit paths only with accurate model attribution; preserve candidate's line ceiling 37396. Focused meaningful regression tests first; no full vitest suite unless a later designated release requires it. node_modules may be a verified directory junction to the retained candidate's identical-lockfile installation; never alter dependencies through it or run npm install/ci in it.

Heavy work: verify High performance; >=4 GiB free RAM at EVERY browser/build entry; acquire %TEMP%/aa-heavy.lock atomically by mkdir, with owner/run metadata, release in finally. One heavy job at a time. Never delete another run's lock or kill owner processes. Headless installed Chrome, stock GPU flags, mute audio; no --ignore-gpu-blocklist, --unsafe-webgpu, --use-angle or software-raster performance claims. Do not use owner :4300. Worker-specific ports below. Keep 180s soak, 180 samples, 299s hard stop, 120ms rejoin bound, 1.5m replication bound, 10s cold admission budget unchanged. No disabling rendering/physics/netcode to hide freezes. Multiplayer proof requires real host and two guest contexts and visible state, not only unit tests. Preserve Quality/Performance gameplay/collision parity.

Visual method: generate a vivid target IMAGE through an authorized available image-generation tool in the visual stage; generated concept is a reference, never evidence of implementation. Recreate its strongest lighting/material/vegetation/terrain cues with smart procedural geometry/textures, instancing/LOD and bounded effects. Compare actual fixed-camera game captures to the reference at the exact built SHA. Report GPU, browser, resolution/DPR, p50/p95/p99 frame time, draw calls, triangles, shader count and memory/disposal. Get same-machine baseline before accepting a visual cost. Physics remains host-authoritative and presentation does not silently change collision. No unlicensed copied assets. Preserve asset provenance.

Write compact STATUS.json at task start and after each meaningful phase (<2KB), REPORT.md with claim states, exact commit and commands/log paths, and final result JSON. Check work/overnight/STOP (absolute task directory in your brief) before each new phase; if present commit/recover partial work and report STOPPED, do not start a new heavy step. All code/tests/artifacts in assigned worktree only; status/result in lane directory allowed. End with ASTRA-LANE-DONE only when your bounded run is actually finished, even if gates remain RED. Report failure as failure, never claim all multiplayer fixed from partial evidence.

Earlier final soak evidence: retained candidate artifacts/astra-c9/paintfix/astra-c9-paintfix-bundle.json and -table.md. 5/8, 180205ms/180 samples, 345 divergences, primarily guest-to-guest missing replicas from second 4 while host receives both fresh. Rejoin guestB damage 0; reload-after-death both false. Respawn/stair-fire/console/scoreboard pass. Continuous authored-respawn-loadout bug fixed already. Paint material.color swatch fixed already. Black house roofs still visible. Do not repeat rejected fixes or just rerun identical source.
