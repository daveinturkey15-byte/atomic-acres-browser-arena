# R5 — Skills and tooling survey: what the post-reset asset-forge lane can bootstrap off

**Lane:** R5 (research only — nothing was edited, built, installed, published or committed).
**Date:** 2026-09-04. **Machine:** dave-gaming-pc. **Worktree:** `C:/Users/david/projects/aa-claude-research`, branch `contrib/dave-gaming-pc/claude/research-2026-09-04`.
**Owner context read:** `docs/PASS84_OWNER_FEEDBACK_2026-09-02.md` rows HF-460..HF-468, including the 2026-09-04 10:35 HF-462 correction (the shared post is **morning-diner**, not Astra) and HF-468 (Astra threads; owner wants a code-native equivalent, Blender optional later on a test map).

**Claim-state key.** `VERIFIED` = I read the file or ran the command in this session. `CLAIMED` = a source asserts it and I recorded the source. `OPEN` = unknown, and named as unknown.

**"The bar", used throughout this document, is two different things and they must not be merged:**

- **The diner bar (an artefact we can inspect).** `StarKnightt/morning-diner`, cloned read-only to `C:/Users/david/projects/morning-diner-ref` at commit `75c37de9b35f313ba9050f76b52674cb677e5491` — VERIFIED. 31,406 lines of TypeScript across `src/`, a 3,646-line `BUILD.md`, an 87-line verbatim `docs/PROMPT.md`, and a **447-line `docs/REFERENCE.md`** that is the most valuable single file in the whole survey (see §3.1).
- **The Astra bar (output only).** Four X threads recorded in HF-468. No repository, no prompt, no tooling list is published for any of them. Per `visual-gauntlet-loop`'s own source boundary and register row 47's wording, **output is a bar, not a recipe.** Anyone who says "the thread shows the method" has not read the thread.

---

## 0. A blocking machine finding, before anything else

**VERIFIED — Claude Code currently discovers zero shared skills on this machine.**

- `C:/Users/david/.claude/skills` is a symlink to `/c/Users/david/.agents/skills` (`ls -la C:/Users/david/.claude/`).
- `C:/Users/david/.agents/skills/` contains exactly one entry: `.system/`, which holds the Codex system skills (`imagegen`, `openai-docs`, `plugin-creator`, `skill-creator`, `skill-installer`, marked by `.codex-system-skills.marker`).
- The canonical store — `C:/Users/david/Documents/desky-bootstrap-clone/Skills` — holds **163 `SKILL.md` files** (VERIFIED by `find ... -name SKILL.md`, excluding `.archive`, `.curator_backups`, `.hub`). None of them are reachable through the `~/.claude/skills` path.

So the premise "our shared skill store, 163 skills" is true of the **vault**, and false of **what this harness can currently load**. Global `CLAUDE.md` describes the junction as pointing into the vault `Skills/` directory; it now points at `.agents/skills`, which the Codex system-skill installer appears to own. The documented repair is `<vault>\_Scripts\link_skills.ps1`, and the standing rule is *never restore a private copy*. This is a machine-config change, so it is an **owner/permission action, not something this lane should do** — but every lane that says "use skill X" is currently wrong unless the agent reads the vault path directly.

**Consequence for the post-reset lane:** reference skills by their **absolute vault path** (`C:/Users/david/Documents/desky-bootstrap-clone/Skills/<category>/<skill>/SKILL.md`), not by name, until the junction is repaired.

Secondary finding (VERIFIED, low severity): `Skills/CATALOGUE.md` says "159 active skills" and omits three that exist on disk — `game-development/comfyui-3d-native-pipeline`, `game-development/open-world-city-art-loop`, `game-development/threejs-webgpu-interior-lighting-look`. The catalogue is generated by `_Scripts/gen_skill_catalogue.py` and has not been regenerated since those three landed. Routing by catalogue alone will miss the two most relevant skills in this survey.

Repo-local skills also exist and are unaffected: `C:/Users/david/projects/aa-claude-research/.agents/skills/` carries nine Atomic Acres skills (`atomic-acres-combat-registry`, `-destructible-world`, `-killstreak-authority`, `-multiplayer-combat-verification`, `-owner-feedback-gate`, `-production-asset-governance`, `-spatial-audio`, `-weapon-viewmodel-forging`, `-webgpu-frame-pacing`) — VERIFIED.

---

## 1. Our own store: every skill relevant to procedural 3D art, three.js/WebGPU/TSL, asset generation, visual QA, Blender, ComfyUI

All paths below are relative to `C:/Users/david/Documents/desky-bootstrap-clone/Skills/`. All frontmatter, line counts and body content are VERIFIED by reading the files.

### 1.1 The four that carry most of the weight

| Skill | Covers | Gap vs the Astra/diner bar |
|---|---|---|
| `game-development/threejs-webgpu-interior-lighting-look` (201 ln, MIT, authored by this lane's predecessor for HF-421) | The closest thing we have to a photoreal-look recipe: value composition (~85% of frame in a narrow desaturated mid-dark band), emissive-above-bloom-threshold fixtures, distance darkening, decal grime, mote field, exactly one exposure event. Names the in-repo consumers by file (`rendering/filmic-grade-chain.ts`, `screen-space-post.ts`, `art-direction.ts`, `atmosphere-system.ts`, `particles/particle-field.ts`, `arena-contrast-lighting.ts`). Carries hard budget table and the two safety fences (`ART_DIRECTION_SAFETY_BOUNDS.bloomThresholdScale` ∈ [1, 1.3], `vignetteBase` ≤ 0.24). | **Interiors and darkness only, and explicitly says the daylight rule inverts.** The diner is a *bright* interior at 08:00 with a hard sun through blinds; Nuke Town's vehicles are exterior daylight hard-surface. This skill's core insight ("check whether the reference is actually buying it with lighting technology") is exactly right and reusable, but it has **no material/surface half** — it stops at "grime as a TSL mask stack" and never authors a material set. |
| `game-development/atomic-acres-procedural-art-authoring` (75 ln, MIT) | The repo-specific procedural-art contract: additive-only file ownership, determinism via `mulberry32`, presentation-only tagging (`userData.farcrysisArt`), triangle budget (<~4k), and a long list of measured Farcrysis coordinate gotchas and reusable recipes (shoreline foam, displaced rock, path ribbon, canvas ground textures, atmosphere module). | **It is a Farcrysis skill wearing a general title.** Nearly every concrete number in it (`ARENA_HALF`, water at y=-0.3, lagoon trough -0.59, sky dome r=200, spawn coordinates) is one arena's. It teaches *how to add scenery to one existing file without breaking it* — the opposite altitude from "author a whole beautiful scene". It has no reference-comparison step and no material-quality bar at all. |
| `quality/visual-gauntlet-loop` (83 ln, MIT) | The bounded builder/critic loop: freeze an inspectable scorecard before a builder sees the work, fresh critic with hidden builder history, blind A/B, one bounded correction per round, stop states (`continue \| refine-spec \| refine-code \| request-input \| stop`), 3 corrections/subsystem and 6 total ceiling, full per-round evidence record, integration/smoothing pass at the end. Source boundary names `somethingbig.ai/gauntlet-loop` and `mshumer/Claude-of-Duty` @ `d9b237b7…` as evidence, not authority. | **The critic scores against a *scorecard*, not against a *reference image*.** That is precisely the delta HF-468 identifies in Astra: (1) reference gathering, (2) a critic that compares the render against the reference. The skill even says "use licensed references, original mood boards, current project baselines or measurable targets" — but it gives no mechanism for *acquiring* a first-party reference set, no side-by-side capture format, and no contract for a vision critic that is handed both images. This is the single biggest authored gap in the store. |
| `game-development/webgpu-tsl-arena-forging` (38 ln) | The arena-level rendering contract everything else sits inside: `RenderRuntime` port, one `ArenaVisualDefinition` per level with atomic load/dispose, WebGPU fail-closed after `renderer.init()`, **zero `ShaderMaterial`/`RawShaderMaterial`/GLSL strings in the WebGPU path**, TSL-only conversion, and the WebGPU proof receipt (backend attestation, normalized WGSL / pipeline-descriptor hash — never `Node.getCacheKey()` alone). | It is a **migration and governance** skill, not an art skill. It tells you what you may not do (which is load-bearing: it is the reason the diner's whole `onBeforeCompile` + `ShaderMaterial` post stack cannot be copied) and nothing about what a beautiful surface looks like. |

### 1.2 The rest of the relevant set

| Skill | One line: what it covers | One line: gap vs the bar |
|---|---|---|
| `game-development/procedural-sdf-raymarched-worlds` (287 ln, MIT) | The route decision (raymarch = screen-space cost vs rasterise = scene-space cost) with a decision table, a language-neutral math core (`sdSphere…`, `opSmoothUnion`, `fbm`, `worley3`, `domainWarp`, tetrahedron analytic normals, Lipschitz `stepScale` 0.5–0.9), Cook-Torrance analytic PBR, and a five-rule ring-fence for adding a raymarch pass to an existing game. | Its own honest default rules it out for the lane: "rasterised procedural geometry is the world; raymarched SDFs are effects and set-pieces inside it". Vehicles need colliders and penetration classes (HF-467), so this is a *set-piece* tool, not the forge. Its **fBM/worley/domain-warp core is directly reusable** as the maths spine of a material library. |
| `game-development/open-world-city-art-loop` (284 ln, MIT) | The environment-art specialisation of the gauntlet loop: street-cell decomposition, the originality boundary (never copy a commercial game; *your own photographs* are the lawful bar), and the load-bearing element ordering by screen area (road surface → pavement/kerb → facade bays → furniture density → traffic-as-dressing → trees → wayfinding). Records the reference's own telemetry at **151 ms / 20 fps** and warns the overcast ambient-dominant grade is a trap that hides weak procedural materials. | It is a *street* decomposition; Nuke Town vehicles are one item ("traffic as dressing") inside it. But its two transferable rules are exactly what the vehicle lane needs: **order the work by screen area**, and **the lawful bar is a photograph you own, not a game frame**. |
| `game-development/ai-3d-asset-generation-loop` (202 ln, MIT) | The three supply routes (code-only procedural / AI-generated mesh / licensed registry) with a cost-of-being-wrong column, the "code-generate the world and the verbs, generate only the hero props" separation, the browser self-verification loop, and the honest caveat that "asset generated" ≠ "asset integrated". | Route-choice governance, not craft. It correctly points at code-only for vehicles, then hands off — there is no "and here is what a convincing car material is". |
| `game-development/img2threejs` (613 ln, **Apache-2.0**, v1.4.4, has `docs/`, `grimoire/`, `ROADMAP.md`) | Rebuilds an object in a reference image as a code-only procedural three.js model, staged sculpt pipeline, quality gates, AI-vision self-correction loop, sockets/colliders/destruction groups. Agent-agnostic. | **The closest existing thing to a code-native Astra, and it is single-object.** It reconstructs *one prop from one image*; it has no whole-scene composition, no material library shared across props, and its own boundary excludes deforming characters and first-person arms. It is the right starting point for "coach, truck, cars" (HF-462) and the wrong one for "whole beautiful scenes". |
| `game-development/atomic-acres-asset-authoring` (173 ln, MIT) | The AI-assisted PBR-texture side of AA asset work. | Assumes generated texture inputs; the owner's direction (HF-462) is explicitly *away* from generation and toward code-native surfaces. |
| `game-development/comfyui-3d-native-pipeline` (246 ln, MIT, authored 2026-09-02) | Concept image → textured game-ready glTF locally using ComfyUI **core** Trellis.2/Pixal3D nodes, mesh post-process, PBR bake, provenance. | Blender-free but not code-native: it produces a mesh file, which the owner's brief moves away from. Correctly scoped in register row 45 to *static hero props only*, never rigged/deforming, and gated behind ~10 GB of weights the owner has not installed. Keep as the fallback route for one hero prop, not the lane. |
| `game-development/threejs-webgpu-water` (338 ln, MIT) | TSL water: spectrum-vs-buoyancy parity, Beer-Lambert absorption, bubble backscatter, persistent foam, shoreline, per-arena rosters, budgets. | Exemplary *shape* for what a material-family skill should look like (physics term → TSL expression → budget → capture). Wrong subject; **copy the structure**. |
| `game-development/threejs-rtx-runtime-route` (706 ln, MIT) | Native-vs-browser ray-tracing decision and how to ship a classic ray-traced preset well. | The interior-lighting skill exists partly to argue this is *not* needed for the look. Read it only to close down a "let's ray trace it" proposal. |
| `game-development/game-animation-asset-pipeline` (361 ln, MIT) | Mocap, text-to-motion, retargeting, H3 sprites, image-to-3D; mentions Blender in the pipeline. | Animation lane, not art lane. |
| `game-development/game-hud-menu-overhaul`, `game-release-benchmark-guard` | UI overhaul contract; immutable-benchmark/rollback discipline. | Adjacent governance; no art content. |
| `software-development/threejs-procedural-vegetation` (443 ln, MIT) | Dense foliage via `InstancedMesh`, PBR materials, deterministic spatial distributions, all procedural, presentation-only. | The best worked example in the store of "many instances, few draw calls, no imported assets". Directly reusable pattern for parked-vehicle families and street furniture. |
| `software-development/additive-module-authoring` (190 ln) | Self-contained additive FX module pattern in strict-TS three.js: creates geometry/materials/animation without touching gameplay objects or colliders. | The right *packaging* for anything the forge emits. |
| `software-development/threejs-frame-loop-audit` (173 ln, MIT) | Frame-loop cost, GPU leaks, visual defects. | The cost check after the budget is missed; no art content. |
| `software-development/threejs-game-development` (122 ln, MIT) | The generalist three.js game skill; carries `references/source-provenance.md` and `references/jungle-trail-case-study.md` and `references/asset-generation-and-bounded-critique.md`. | The register embeds StarKnightt rows 7/8/35 here — so this is where a **morning-diner case study belongs if we do not author a new skill**, and I recommend against that (see §4). |
| `software-development/realtime-browser-qa` (215 ln, v1.3.0, MIT) | Playwright/browser QA for frame-driven apps, whole-world mechanical sweeps, traversability and draw-call budgets; references include `native-edge-webgpu-cold-warm-gate.md`, `native-gpu-browser-evidence.md`. | Mechanical capture discipline, which is exactly what a reference-comparison critic needs underneath it. It has no image-comparison verb. |
| `software-development/browser-game-runtime-debugging`, `browser-game-dev-server-troubleshooting` | Freezes, textures, input; Vite/WebGPU secure-context gating, port discovery on Windows. | Support skills for the lane's inner loop. |
| `creative/comfyui` (633 ln, MIT, v5.2.0) + `creative/comfyui-request-pipeline` (182 ln) | Local ComfyUI operation and a cron/file-watcher request pipeline (no firewall, no persistent server). | Image generation, which the owner's brief deprioritises for assets — but it is the obvious engine for **generating first-party reference plates** if photographs are not available. That use needs an explicit owner decision (it is generation, and it competes for the owner's GPU). |
| `creative/visual-web-artifacts` (63 ln, MIT) | Browser-deliverable HTML/SVG/p5.js artifacts, design tokens. | The right tool for publishing a side-by-side reference/capture contact sheet. |
| `creative/touchdesigner-mcp` | TouchDesigner via an authorized MCP; its `references/3d-scene.md` mentions Blender. | Out of lane. |
| `mlops/generative-model-provenance` (87 ln) | Licence/VRAM/provenance discipline for downloadable generative models. | The gate any generated reference plate must pass. |
| `mlops/local-vision-fallback` | Local Qwen-VL on `llama-server` as a vision fallback when an external vision API is rate-limited. | **Load-bearing for a reference-grounded critic**: the critic needs a vision model that can be handed two images. Note the memory gotchas: OMP's daemon steals port 8080, and a model that does not fit still loads and pages to system RAM at ~2 t/s. |
| `devops/skills-hub-operations` | Hub endpoints, install command format, weekly scan; see §2. | — |

### 1.3 Blender coverage in our store — VERIFIED: there is none worth the name

`grep -ril blender` over the store returns 30 files, and **not one is a Blender skill**. The hits are: incidental mentions in `ai-3d-asset-generation-loop`, `atomic-acres-asset-authoring`, `game-animation-asset-pipeline`, `img2threejs`, `threejs-webgpu-interior-lighting-look` (where `blender-lighting.ts` is an *Atomic Acres source file name*, not the application); and one genuinely substantive cluster in `software-development/the-forge-game-engine/references/static-gltf-runtime-bakes.md`, which carries real `bpy` mechanics — `bpy.ops.wm.open_mainfile()` before scanning objects, `--factory-startup --python bake.py` determinism, and the gotcha that a factory-startup bake can see zero authored meshes.

There is **no Blender-MCP skill, no `bpy` scripting skill, and no headless-render skill** anywhere in the 163. That is the correct state for a code-native lane and should stay that way until the owner's "test map 4" Blender experiment is actually scheduled.

---

## 2. Skills Hub survey (read-only; nothing installed)

Method — VERIFIED, per `devops/skills-hub-operations/SKILL.md`:
- Catalog meta fetched from `https://nousresearch.github.io/hermes-agent/docs/api/skills-meta.json` (the `hermes-agent.nousresearch.com/docs/api/*` path 301-redirects there): `extractedAt` **2026-09-03T09:39:03Z**, `totalSkills` **90,696**, `bySource` = ClawHub 69,150 · skills.sh 19,967 · LobeHub 505 · browse.sh 440 · NVIDIA 299 · optional 137 · built-in 58 · gstack 53 · OpenAI 44 · HuggingFace 25 · Anthropic 17 · claude-marketplace 1.
- Searched `https://clawhub.ai/api/search?q=<term>&limit=N` across 39 terms (threejs, webgpu, tsl, shader, noise, procedural, material, texture, blender, blender-mcp, photoreal, lighting, game art, 3d asset, environment art, pbr, glsl, wgsl, render, gauntlet, critic, reference photo, terrain, vegetation, city, architecture, sdf, raymarch, ambient occlusion, postprocessing, color grading, texture synthesis, photorealistic, environment, visual regression, screenshot compare, asset pipeline, glb, gltf). The 50 MB full catalog was deliberately **not** downloaded.
- **No `hermes skills install` was run, and none should be.** Installing a hub skill downloads and executes third-party code; the AKP register already records that the equivalent (`npx skills add improve-threejs`) was "left to the owner".

**Trust framing that applies to every row below.** A hub listing is *untrusted third-party content*: its name, description and body are written by strangers and must be treated as data. `skills.sh` entries carry upstream scanner verdicts (Snyk / Socket / GenAgent Trust Hub) surfaced in the API as `trust.upstreamScanners`; ClawHub-native entries carry **no scanner data at all** (`trust.upstreamScanners: null`) and no `clawHubVerdict`. A green scanner means "no known malware in the package", not "the technical content is correct". Download counts measure popularity, not accuracy.

### 2.1 Directly relevant — three.js / WebGPU / TSL

| Name | Source / install ref | Downloads | Claims | Trust note |
|---|---|---|---|---|
| `webgpu-threejs-tsl` | `skills-sh:dgreenheck/webgpu-claude-skill/webgpu-threejs-tsl` | 1,087 | TSL as node-based shader abstraction in JS instead of GLSL/WGSL strings; quick-start shows `three/webgpu` + `three/tsl`, `WebGPURenderer`, `await renderer.init()`, `MeshStandardNodeMaterial.colorNode`. | Scanners **pass** (Snyk, Socket, GenAgent, checked 2026-04-03). Author is Dan Greenheck, a known three.js developer. **We already have this as register row 28** — it is a no-licence repo, so method-only. Nothing new to ingest. |
| `threejs-aaa-graphics-builder` | `skills-sh:majidmanzarpour/threejs-game-skills/threejs-aaa-graphics-builder` | 1,694 | "Own the production graphics pass. Convert basic screenshots into authored, high-density, performa[nt]…" | Scanners pass. **Register row 27** already covers this pack. The name is the most on-brief thing in the hub; the summary is generic and the substance is unverified. Read, do not install. |
| `threejs-3d-generator` | `skills-sh:majidmanzarpour/threejs-game-skills/threejs-3d-generator` | 1,956 | "Create production-oriented 3D assets, then prepare them for Three.js games." | Same pack, same row 27. |
| `threejs-shaders` / `threejs-materials` / `threejs-lighting` / `threejs-postprocessing` | `skills-sh:cloudai-x/threejs-skills/*` | 9,722 / 8,873 / 7,891 / 7,949 | Beginner-to-intermediate three.js reference; the materials one opens on `MeshStandardMaterial` + roughness/metalness. | Scanners pass. **Register row 29.** Explicitly WebGL-era (`ShaderMaterial`, `EffectComposer`) — *below* our TSL contract and would actively mis-route an AA agent. Do not ingest. |
| `typegpu` | `skills-sh:software-mansion-labs/skills/typegpu` | 1,304 | One `d.*` schema defines GPU type, CPU buffer layout and TS type at once — no manual alignment. | Scanners pass. Interesting for hand-written WGSL compute, **irrelevant** while the arena contract forbids non-TSL shader paths. |
| `shader-dev` ("Shader Craft") | `skills-sh:minimax-ai/skills/shader-dev` (mirrored as `nexu-io/open-design/shader-dev`) | 2,229 | "36 GLSL shader techniques (ShaderToy-compatible)" — raymarching, SDF modelling, fluid, particles. | Scanners pass. GLSL/ShaderToy, not TSL. Our `procedural-sdf-raymarched-worlds` already carries the same maths in a language-neutral form with the Lipschitz and analytic-normal discipline these packs typically omit. |
| `shader-programming` / `procedural-gen` / `create-game-assets` / `threejs-materials-lighting` / `threejs-scene-setup` / `threejs-gltf-loading` | `skills-sh:gamedev-skills/awesome-gamedev-agent-skills/*` | 2,234 / 2,357 / 1,329 / 1,757 / 1,759 / 1,793 | Cross-engine shader pipeline concepts; determinism-first procgen from a seed; "turn visual intent into a consistent engine-ready asset set, treat image generation as one production tool". | Scanners pass. The `procedural-gen` determinism framing matches ours; `create-game-assets` is the only hub row that frames image-gen correctly as *one* tool. Worth **reading**; neither adds anything our store lacks. |
| `glsl-encyclopedia` | `clawhub:kklouzal/glsl-encyclopedia` | 720 | GLSL spec/syntax lookup, built-ins. | ClawHub-native, **no scanner data**. A spec index, not a technique. Skip. |
| `gles-rendering-expert-skill` | `clawhub:oahc09/gles-rendering-expert-skill` | 2,397 | Enforces OpenGL ES 3.x API boundaries, TBDR bandwidth optimisation. | No scanner data. Mobile-GL; out of lane. |
| `r3f-*` (fundamentals / materials / lighting / textures / postprocessing / animation) | `skills-sh:enzed/r3f-skills/*` | 1,167–1,357 | React Three Fiber; each begins "inspect the installed renderer and versions first". | Scanners pass. AA is not R3F. The *version-first* opening line is a good habit and nothing more. |
| `threejs` | `clawhub:ivangdavila/threejs` (3,271) · `skills-sh:nexu-io/open-design/threejs` (2,797) · `skills-sh:mrgoonie/claudekit-skills/threejs` (1,199) · `clawhub:openlark/threejs-3d` (917) | — | Generic three.js references. | The ClawHub ones have no scanner data. All are strictly weaker than `software-development/threejs-game-development` + our specialists. |

### 2.2 Procedural texture / PBR material

The single most informative result: **`procedural texture` returns zero hub results** (VERIFIED — `{"results":[]}`). Across 90,696 catalogued skills there is no procedural-texture skill. The nearest neighbours are all image-generation products: `clawhub:blammectrappora/texture-art-generator` (715, "hyperrealistic material images — glass, crystal, liquid, jelly, wax"), `clawhub:dlazyai/material-enhancement` (318, image sharpening), `clawhub:tianheihei002/material-generator` (665, generates **Unreal** material node graphs from natural language), `skills-sh:unity-technologies/skills/shader-graph-create-custom-node` (1,636, official Unity, HLSL custom nodes for Shader Graph), `clawhub:toilanguyen2910/game-dev-art-bible` (374, style guides / colour palettes / reference boards — a *documentation* skill, closest in spirit to our reference-brief idea), and `clawhub:beermanzz/film-lighting-bible` (3,244, a Chinese-language cinematic-lighting prompt handbook for AI image/video generation).

**Finding: the thing the lane needs does not exist on the hub and must be written.** That is a clean answer to "check the hub before authoring from scratch".

### 2.3 Blender / Blender-MCP

| Name | Install ref | Downloads | Claims | Trust note |
|---|---|---|---|---|
| `blender-mcp` | `skills-sh:vladmdgolam/agent-skills/blender-mcp` | 1,730 | Tool-selection guidance: structured MCP tools (`get_scene_info`, `screenshot`) for inspection, `execute_python` for everything else. | Scanners pass. The only *scanned* Blender-MCP skill. It is a thin wrapper over `ahujasid/blender-mcp` (§3.3). |
| `Blender Mcp` | `clawhub:flayzz/blender-mcp` | 3,410 | "Advanced bridge to Blender via MCP… running custom BPY code in real-time." | No scanner data. |
| `Blender` | `clawhub:ivangdavila/blender` | 3,791 | Common Blender mistakes: transform application, modifier order, UV seams, game-engine export settings. | No scanner data. Genuinely useful *content class* (export hygiene) if Blender ever runs on test map 4. |
| `Blender - Agents Help with 3D work` | `clawhub:sfkislev/blender-flue` | 2,460 | Controls Blender from the shell via "Flue", a Python bridge to `bpy` **without** an MCP server. | No scanner data. Architecturally interesting (no server, no socket) and therefore lower blast radius than MCP. |
| `Blender Bpy Enhanced` | `clawhub:emergencescience/blender-bpy-enhanced` | 885 | Procedural modelling, **material nodes**, lighting, rendering, asset generation via `bpy`. | No scanner data. |
| `blender-add-on-development` | `clawhub:seekerzero/blender-plugin-dev` | 1,255 | Add-on/plugin dev for Blender 4.x/5.x. | No scanner data. |
| `Blender 3d Modeling` | `clawhub:agentpmt/blender-3d-modeling` | 579 | Headless Blender **in the cloud**, turntable renders. | No scanner data. Cloud execution — a data-egress question, not just a code question. |
| `Openclaw 3d Blender MCP` | `clawhub:yejay7/openclaw-3d-blender-mcp` | 839 | Full Blender-MCP install incl. **ngrok** tunnelling. | No scanner data. An install recipe that opens a public tunnel to a local process; **do not run**. |

### 2.4 Visual QA / critic / reference comparison

| Name | Install ref | Downloads | Claims | Trust note |
|---|---|---|---|---|
| `Screenshot` | `clawhub:ivangdavila/screenshot` | 17,606 | Capture, inspect and **compare** screenshots of screens, windows, regions, web pages, simulators and CI runs. | No scanner data. The highest-download graphics-adjacent skill found; two near-identical clones exist (`zhouhuihui008/screenshot-1-0-1`, `subaru0573/super-screenshot`), which is itself a signal about hub hygiene. |
| `playwright-visual-testing` | `skills-sh:manutej/luxor-claude-marketplace/playwright-visual-testing` | 1,233 | Browser automation + visual testing. | Scanners pass. Weaker than `realtime-browser-qa`. |
| `Visual Qa` | `clawhub:kbo4sho/visual-qa` | 1,129 | Pixel-level visual regression, capture/compare, gate deploys. | No scanner data. |
| `Critic Agent` / `Architecture Critic` / `work-critic` / `Honest Critic` | ClawHub, various | 623–799 | Generic adversarial-review agents scoring outputs. | No scanner data. **None of them compare a render against a reference image** — they score text against rubrics. Same gap as our own `visual-gauntlet-loop`. |
| `nm-gauntlet-*` (`curate`, `onboard`, `extract`, `graph-search`, `graph-build`) | `clawhub:athola/*` | 1,300–1,459 | A code-knowledge-graph onboarding tool. | No scanner data. **Name collision only** — nothing to do with Matt Shumer's gauntlet loop. Recorded so nobody re-investigates. |

**Overall hub verdict: nothing here should be installed.** Of ~30 relevant hits, every one is either (a) already a register row (27, 28, 29), (b) WebGL-era and below our contract, (c) an image-generation product, or (d) a Blender bridge we do not want yet. The hub's value in this lane was **negative evidence**: it proved the procedural-texture/reference-critic skill does not exist and must be ours.

---

## 3. Public resources (WebFetch; all CLAIMED unless marked)

### 3.1 The diner reference — the most valuable thing found (VERIFIED, read locally)

`C:/Users/david/projects/morning-diner-ref` @ `75c37de9b35f313ba9050f76b52674cb677e5491`, remote `https://github.com/StarKnightt/morning-diner.git`.

**Licence — VERIFIED: there is NO licence file at this revision.** `ls -a` at the repo root shows `BUILD.md`, `README.md`, `docs/`, `src/`, `tools/`, `public/`, config files, and no `LICENSE` / `LICENSE.md` / `LICENSE.txt` / `license`. This is the same position as register row 35 (`gas-station-highway`, "NO LICENSE FILE… although the README summary claims MIT"), and the same authority applies: **learn the technique, never copy the expression.** No file, no snippet, no `docs/REFERENCE.md` table may be pasted into Atomic Acres.

**Architecture — VERIFIED, and the finding is a hard constraint on reuse:**

- It is **WebGL2, not WebGPU.** `grep -rn "WebGPURenderer\|three/webgpu\|three/tsl" src` returns **nothing**; `THREE.WebGLRenderer` appears throughout (`src/capture/pose.ts`, `src/core/compile.ts`, `src/core/quality.ts`). `package.json` pins `three ^0.185.1` with `@types/three`, `playwright 1.62.1`, `pngjs`, `vite ^8.2.2`.
- Its post chain and several materials are **`ShaderMaterial` + `onBeforeCompile`**: `src/post/PostPipeline.ts` builds 9 `ShaderMaterial` passes (haze, composite, prefilter, blur, down, finish, finish-no-grain, grain), `src/post/Dust.ts` and `src/post/Steam.ts` each own one, and `src/core/materials.ts` (lines 251, 575, 744), `src/interactions/Pour.ts` (323, 358, 386) patch stock materials through `onBeforeCompile`, with a `src/core/shaderPatches.ts` installer to reach clones and merged buckets. **Every one of those routes is forbidden by `webgpu-tsl-arena-forging` in the AA WebGPU path.**
- Its **texture route is portable, and this is the key insight.** `src/procedural/textures.ts` (2,603 lines) authors surfaces with the **2D canvas API** — `OffscreenCanvas` when `document` is undefined, `HTMLCanvasElement` otherwise, "same 2D API, same rasteriser, byte-identical output" — and returns `{ map, roughnessMap, normalMap }` triples, deriving the normal map from a wrapped height field by central differences (`normalFromHeight`). `src/core/textureBank.ts` (420 lines) proxies those generators onto **Web Workers**: `bank.proxy(texModule, "tex")` returns placeholder textures immediately, the worker rasterises on an `OffscreenCanvas`, and `applyProps` lands the `ImageBitmap` and sampler state when it replies — with a synchronous main-thread fallback for generators not in `SHAPES`, and a documented guarantee of pixel-identical output because every generator seeds its own PRNG. **`CanvasTexture` is backend-agnostic; this whole layer would work unchanged under `WebGPURenderer` with `MeshStandardNodeMaterial`.**
- Scale of the material set: `src/core/materials.ts` (862 lines) declares a `Palette` with ~40 named materials (`wallPaint`, `ceilingTile`, `vinylRed` + `vinylRedCrazed` as `MeshPhysicalMaterial`, `formica`, `formicaCounter`, `chrome`/`chromeWorn`/`chromeWorn2`/`chromeSoft`/`chromeBrushed`, `ceramic`, `bisque`, `coffeeStain`, `capWood`, `laminatePanel`/`laminateCabinet`/`laminateScuffed`, …), with vertex colours carrying edge wear on the vinyl bucket. `src/scene/Lighting.ts` is 1,713 lines; `src/scene/Exterior.ts` (the cars and the lot) is 2,135.
- It ships its own capture harness: `tools/shoot.mjs`, `sequence.mjs`, `crop.mjs`, `gpu.mjs`, `post-bench.mjs`, plus `src/capture/pose.ts` exposing `__APP` and a `gpuRendererString()` for renderer identity in the shot.

**`docs/REFERENCE.md` (447 lines) is the Astra "reference gathering" step, done in text instead of photographs — and it is better than photographs for our purposes.** It is a *physically-grounded technical brief*: sun elevation 31.1° / azimuth 82° at 08:00 MST on 20 June at Flagstaff (with the equation-of-time correction and three citations), sun angular diameter 0.53° → **penumbra widens 9.3 mm per metre of occluder-to-surface distance**, relative air mass 1.93, a profile-angle table β = atan(tan 31.1° / cos γ) for five window orientations with floor-hit distances, a venetian-blind blocked-fraction formula `f = w·|sin(β−θ)| / (p·cos β)` tabulated for tilts 0–40°, 29 slats over a 1.25 m window, and the conclusion that **"CG that keeps stripes razor-sharp to the far edge reads as fake"** with the fix named (PCSS or receiver-distance-proportional blur). Every estimate is explicitly marked *(est.)*.

`docs/PROMPT.md` (87 lines, verbatim owner brief) confirms the one-page-brief pattern already recorded as register row 35: atmosphere pin ("indistinguishable from a real photo… Terrence Malick / Gregory Crewdson"), an **absolute verb whitelist** ("Exactly 3 interactions. No more. No less. No extra interactions sneak in during development"), the all-procedural clause ("Nothing downloaded. Nothing imported. Everything from math"), an explicit **sequential** build order of 8 named systems with "do NOT fan out multiple sub-agents in parallel — my machine can't handle it", and a negative-space list ("No menus, no food, no plates of eggs… No film grain… No lens flare. No bloom").

`BUILD.md` is 3,646 lines of build log — i.e. the loop's own evidence record, which is the artefact our `visual-gauntlet-loop` demands and which most public projects do not publish.

### 3.2 three.js TSL — the API we actually target

- **TSL specification** — <https://github.com/mrdoob/three.js/wiki/Three.js-Shading-Language> and <https://threejs.org/docs/TSL.html>. CLAIMED from the wiki: node-material properties `colorNode`, `roughnessNode`, `metalnessNode`, `normalNode` (view-space), `emissiveNode`, `positionNode` (vertex displacement), `envNode`; `MeshPhysicalNodeMaterial` adds `clearcoatNode`, `sheenNode`, `iridescenceNode`, `transmissionNode`, `thicknessNode`, `anisotropyNode`. Control flow: `Fn()`, `Loop()` with `Break()`/`Continue()`, `If/ElseIf/Else`, `Switch/Case`. Sampling: `texture()`, `textureLoad()`, `textureSize()`, **`triplanarTexture()`**, `cubeTexture()`, `texture3D()`. Helpers: `rotateUV()`, `spherizeUV()`, `spritesheetUV()`, `equirectUV()`, `oscSine/Square/Triangle/Sawtooth`, `remap()`, `remapClamp()`, `saturation()`, `vibrance()`, `hue()`, `posterize()`, `grayscale()`, `packNormalToRGB()`, `unpackRGBToNormal()`.
- **The gap that decides the plan:** the spec lists only `hash(seed)` and `range(min,max)` for randomness. **There is no built-in Perlin/simplex/fBM/worley in TSL** — the wiki points at the third-party TSL-Textures addon instead. So a procedural material library in TSL means *we* own the noise core. Our `procedural-sdf-raymarched-worlds` already specifies exactly that core (`hash3`, `valueNoise3`, `gradientNoise3`, `fbm`, `ridgedFbm`, `domainWarp`, `worley3`) as a language-neutral spec — CLAIMED-to-VERIFIED bridge: the spec exists in our store; whether an equivalent already exists in AA's `src/map3/noise.ts` (157 lines) is **OPEN** and is the implementer's first read.
- Official TSL examples worth reading (CLAIMED, from search): `https://threejs.org/examples/webgpu_tsl_procedural_terrain.html` and `https://github.com/mrdoob/three.js/blob/master/examples/webgpu_tsl_wood.html` (procedural wood, by Logan Seeley after a Lance Phan Blender tutorial — i.e. a Blender material graph transliterated into TSL, which is precisely the "Blender result without Blender" move HF-462 asks for), plus `webgpu_materials.html`.
- **`boytchev/tsl-textures`** — <https://github.com/boytchev/tsl-textures>. CLAIMED: a collection of real-time procedural **3D** TSL textures (polka dots, planet surfaces, normal maps, wood, watermelon, protozoa, texture-in-motion), requires the WebGPU build of three.js and `WebGPURenderer` + node materials, **MIT licence**, `npm install three tsl-textures`, imported per-texture (`import { polkaDots } from 'tsl-textures/polka-dots'`) and assigned to `colorNode`. This is the only ready-made TSL procedural-material library found anywhere in the survey and the only external package I would even consider.

### 3.3 Procedural-noise libraries in JS and their licences (all CLAIMED from the repos)

| Library | What it gives | Licence |
|---|---|---|
| `Auburn/FastNoiseLite` — <https://github.com/Auburn/FastNoiseLite> | OpenSimplex2, OpenSimplex2S, Cellular/Voronoi, Perlin, Value, Value-Cubic; domain warp and fractal (fBM) options, 2D and 3D. Ports include **JavaScript/TypeScript (npm)** and **HLSL and GLSL**. | **MIT** |
| `stegu/webgl-noise` — <https://github.com/stegu/webgl-noise> | GLSL simplex 2D/3D/4D, classic Perlin 2D/3D/4D incl. periodic, cellular/Worley, and `psrdnoise` (tiling simplex with analytical derivatives and gradient rotation). No setup, no texture lookups; works from WebGL 1.0 up. Ashima Arts + Stefan Gustavson. | **MIT** (OSI-approved, stated in the README) |
| `jwagner/simplex-noise.js` | The de-facto JS simplex implementation. | **MIT** — CLAIMED from general knowledge, **not fetched this session; OPEN, verify before use.** |
| Inigo Quilez's SDF/noise articles (`iquilezles.org`) | The canonical primitives/combinators/fBM formulations our SDF skill already transliterates. | **OPEN** — per-article, usually MIT with attribution; verify per snippet before any copy. |

Note on patents (CLAIMED, verify before relying on it): Ken Perlin's simplex-noise patent (US 6,867,776) is generally reported as expired since January 2022, which is why OpenSimplex-style workarounds are no longer necessary. Treat as background, not as a legal opinion.

### 3.4 The Blender-MCP ecosystem

`ahujasid/blender-mcp` — <https://github.com/ahujasid/blender-mcp>. CLAIMED: a socket-based MCP server for Blender giving scene inspection, object create/modify/delete, material control, arbitrary Python execution, and asset integration via Poly Haven, Sketchfab, Poly Pizza, Hyper3D Rodin and Hunyuan3D. **MIT licence.** Its own README warns that `execute_blender_code` "allows running arbitrary Python code in Blender, which can be powerful but potentially dangerous… **ALWAYS save your work before using it**", and describes a "safe mode" that validates scripts and blocks file access and network operations.

**Published "AI in Blender" prompt best practices: I found none that qualify.** The hub's Blender skills are install recipes and mistake-lists; the Astra threads publish no prompt. The only *substantive* practice found in this survey is our own, in `the-forge-game-engine/references/static-gltf-runtime-bakes.md`: make the bake entry point deterministic (pass the `.blend` on the command line or call `bpy.ops.wm.open_mainfile()`), require `--factory-startup --python bake.py` to work from an empty scene, and keep mesh/primitive/detail floors strict rather than weakening them so an empty scene passes. **Status: OPEN** — if the owner schedules test map 4, this is a research question, not a solved one.

### 3.5 Matt Shumer's gauntlet loop — public, and already ours

<https://somethingbig.ai/gauntlet-loop>, Matt Shumer, 27 July 2026 (CLAIMED). Roles: a **lead** decomposes the goal into independently improvable pieces; **builders** produce each piece; **critics** with fresh context judge against a reference standard. Steps: establish a concrete quality bar (his example is *real Call of Duty screenshots*), decompose to the smallest separately-judgeable unit, build, critique with blind A/B where possible, return the largest remaining gap, repeat. Stop when parity with the bar is reached, when margins go negligible, or when the operator halts. Claim: ~55,000 lines of code with every texture, mesh, animation and sound generated from scratch from a single prompt, attributed to the method rather than the model.

Two things to carry forward: (1) our `visual-gauntlet-loop` already encodes this **plus** a stricter bounded policy (3 corrections per subsystem / 6 total, explicit stop states, retained regression baselines) — nothing to ingest; (2) **his bar is a copyrighted game screenshot, and ours may not be.** Register row 47 already settled this: the bar we show a critic must be something we may lawfully hold.

### 3.6 Astra (HF-468) — recorded as a bar

Four X threads, read in-pane without login per HF-468: `mattshumer_/2095609734845927525` (Manhattan in Unreal over a week, street by street); `Stefan_3D_AI/2095720649922871630` (OpenAI launch 4 Sept 2026 — GPT-6 Astra works inside Blender autonomously: gathers reference photos, builds the scene, renders test frames, checks them against the references, fixes what is off, ships to UE5 as a walkable level), `…630695029` (Palace of Fine Arts rebuilt from hundreds of photos by render-and-compare), `…944115856` (design drawing → UE5; Playco greybox → three playable prototypes). All CLAIMED, from promotional posts, one of them a product launch. **No repository, no prompt, no pipeline.** The extractable delta versus what we own is exactly two things and the ledger already names them: **reference gathering** and **a critic that compares renders against references rather than against a rubric.**

---

## 4. Recommendations — what to ingest, what to write, and the licence stance

### 4.1 Ingest as skills now: **nothing from the hub, nothing from the public web.**

Justification, not laziness: of ~30 relevant hub hits, three are already register rows (27 majidmanzarpour, 28 dgreenheck, 29 CloudAI-X), the three.js/material ones are WebGL-era and would mis-route an agent bound by a TSL-only contract, the "procedural texture" query returns literally zero results, and every Blender bridge is either unscanned, cloud-executing or ngrok-tunnelling. Installing any of them downloads and executes third-party code into a store that is **shared live with Codex, OMP, dsh and Hermes** — the shared-skill-store rule makes that a governed drift event, not a convenience.

One conditional exception, for the **owner** to decide, not this lane: **`boytchev/tsl-textures` (MIT)** is the only external package worth evaluating, and even then as a *reading source* for TSL idioms first and a dependency second. Adding it as a runtime dependency touches `assets.manifest.json` / `qa:asset-provenance` and the "no external asset downloads" clause in the art contract.

### 4.2 Write ourselves — three artefacts, in this order

1. **A new skill: `game-development/reference-grounded-asset-forge`** (canonical store, then AKP-governed per `skill-regression-policy.json` with a paired evaluation record). It carries the exact two things §3.6 identifies as missing and nothing else:
   - **The reference brief format**, modelled on the diner's `docs/REFERENCE.md` *method* (never its text): for each subject, a physically-grounded brief with real measured quantities, each line cited or marked *(est.)* — for a vehicle that means body dimensions, panel-gap widths in mm, paint gloss/clearcoat roughness ranges, glass transmission and tint, tyre sidewall geometry, chrome vs painted-steel Fresnel behaviour, and the wear vocabulary (stone chips at the leading edge, sill dirt line, sun-bleached horizontal surfaces, rust bloom at seams).
   - **The reference-grounded critic contract**: the critic is handed the reference plate *and* the capture side by side, at the same framing, and must name the largest gap **in reference terms** ("the sill dirt line is absent", not "add more grime"). Blind A/B where the bar is a project baseline.
2. **A study doc in the repo: `docs/technique-studies/morning-diner-procedural-materials.md`**, written the way `subway-scene-lighting-look.md` was — falsifiers first. The three falsifiers this survey already establishes: it is **WebGL2** (no `three/webgpu` import anywhere); its post chain is **nine `ShaderMaterial` passes**, so none of it ports into our WebGPU path; and its texture layer is **canvas-2D in Web Workers**, which *does* port. Method transfers, expression does not.
3. **Register rows.** The register's top row is **50** (VERIFIED, `grep "^### "`), so: **row 51 = morning-diner** (owner-shared `x.com/prasenx` 2026-09-03 per HF-462; canonical `StarKnightt/morning-diner @ 75c37de9b35f313ba9050f76b52674cb677e5491`; **licence: NO LICENSE FILE at the pinned revision** — same posture as row 35; embedded in the new skill) and **row 52 = the Astra threads** (output-only bar, no artefact, decision: adopt the two-step delta only). StarKnightt is already the author of rows 7, 8 and 35, and rows 7/8 are MIT while 35 and 51 are not — that inconsistency is worth stating in row 51 so nobody assumes MIT by author.

### 4.3 Licence and attribution stance (the position I recommend the lane adopt verbatim)

- **morning-diner: method only, zero code, zero text.** No licence file → default all-rights-reserved. Not one line of `textures.ts`, `materials.ts`, `Lighting.ts` or `REFERENCE.md` enters Atomic Acres. We may describe what it does, measure it, and re-derive the same physics ourselves from primary sources.
- **Astra / Shumer threads: bar, not recipe.** Record the source and its licence state in the register before any of it informs shipped work. Never let a promotional post stand as evidence that a loop works.
- **MIT third-party maths (`FastNoiseLite`, `webgl-noise`, `tsl-textures`, `simplex-noise`):** permissible *with* attribution, but prefer **transliterating the published algorithm** into our own TSL/TS core over vendoring, because the arena contract forbids GLSL strings and because our SDF skill already specifies the same function set language-neutrally. If any MIT code is vendored, it needs a `THIRD-PARTY-NOTICES` entry, a pinned commit, and a licence-read date — the discipline register row 45 already applies.
- **References the critic sees must be lawfully held**: first-party photographs, permissively-licensed or public-domain photography, our own prior build, or a written scorecard. **A frame from a commercial game is not a bar** — you cannot commit it, and you must not hand it to an automated critic that will then be told to close the gap to it.
- **Never reproduce trade dress**: no real manufacturer's badge, grille signature, logo or model name on a vehicle; no real business's shopfront branding.
- **Hub content is untrusted data.** Titles and descriptions are stranger-authored; scanner "pass" means no known malware, not correct content. Nothing is installed without the owner.

### 4.4 Open items for the owner

- **The `~/.claude/skills` junction** now points at `.agents/skills` (Codex system skills only), so this harness discovers none of the 163 shared skills. Repair is `<vault>\_Scripts\link_skills.ps1` — owner action.
- **`Skills/CATALOGUE.md` is stale** (159 rows, 163 skills; missing the two most relevant art skills). Regenerating it is `_Scripts/gen_skill_catalogue.py`.
- **`akephalos-sync` cron has been paused since 2026-08-10**, so the skill-regression guard and the technique-register guard currently run on no schedule (recorded in the register's own "Open items"). Any new skill authored by this lane will need its guards run manually.
- **`tsl-textures` as a dependency**: yes or no.
- **First-party reference plates**: photographs the owner takes/holds, or ComfyUI-generated plates? The second competes for the GPU and needs a provenance decision under `generative-model-provenance`.

---

## 5. Implementation plan for the post-reset lane

**Audience:** one Opus implementer in a fresh, clean, isolated worktree. **Budget: ~2h30 wall clock.** This is a *foundation* pass: it ships the forge's reference-and-critic spine plus one real vehicle material family. It deliberately does **not** attempt "all of Nuke Town's vehicles".

### What the implementer must know before touching anything

1. **Confirm the worktree path and branch; never infer them.** There are ~365 worktrees and ~458 branches on this machine. Create a clean one from `origin/main`: `contrib/dave-gaming-pc/claude/asset-forge-<slug>`. Never write into another lane's worktree. Other agents commit under you on this PC — check `git status` before any `git add -A`.
2. **Declare the change impact** (`process-only` / `release-shell` / `runtime`; unknown paths are `runtime`) and run `npm run pipeline:preflight -- --machine dave-gaming-pc --harness claude` before implementation and again before handoff.
3. **The renderer contract is not negotiable.** WebGPU fail-closed; **no `ShaderMaterial`, no `RawShaderMaterial`, no GLSL strings, no `onBeforeCompile`** in any art path. `src/farcrysis-art.ts` and the equivalent gate files accept `MeshStandardMaterial` / node materials only. This is why the diner's post chain is off-limits and its texture bank is not.
4. **Presentation-only, additive, deterministic.** No colliders, spawns, navigation or gameplay authority from anything this pass emits. Seeded PRNG only — no `Math.random` in new code, no `Date`, no iteration-order dependence.
5. **Machine rules.** Headless browsers only (a guard kills headed Playwright profiles on `dave-gaming-pc` within ~15 s); if you must run one, `--window-position=2560,0`. Check `http://127.0.0.1:8188/queue` and require ≥3000 MiB free VRAM before any measurement — the owner's ComfyUI and ollama share this box, and numbers taken while it generates are void. Never kill a process you did not start.
6. **Skills are currently undiscoverable by name** (§0). Open them by absolute path under `C:/Users/david/Documents/desky-bootstrap-clone/Skills/`.
7. **Never weaken a gate to get green.** A correct failure stays failing and its row stays OPEN.
8. **This lane does not publish**, does not push `main` or `gh-pages`, does not merge its own PR, and does not run `npm run deploy`.

### Step 0 — Ground (15 min)

- Read, in this order: `C:/Users/david/Documents/desky-bootstrap-clone/Skills/quality/visual-gauntlet-loop/SKILL.md`, `.../game-development/threejs-webgpu-interior-lighting-look/SKILL.md`, `.../game-development/open-world-city-art-loop/SKILL.md` §4 (screen-area ordering) and §3 (originality boundary).
- Read `src/rendering/surface-forge.ts` **in full** (922 lines) — it already implements the single-`SurfaceDescription`→four-map contract, `createSurfaceNoise`, `sharedMicroDetailRaster()`, `sharedMacroVariationRaster()`, `MICRO_TILE_METRES = 0.25`, `rasterizeSurface`, `forgeSurface`, `surfaceStandardMaterial`, `surfaceTexelBudget`, `disposeSurfaceForge`. **Do not build a second forge.**
- Read `src/map3/noise.ts` (157 lines) and record, as a VERIFIED line in your report, which of `hash3 / valueNoise3 / gradientNoise3 / fbm / ridgedFbm / domainWarp / worley3` already exist in the repo and which do not. This is currently OPEN and everything in Step 3 depends on the answer.
- Sanity-read `C:/Users/david/projects/morning-diner-ref/src/core/textureBank.ts` for the worker-offload shape **only**. Copy nothing.

**Gate:** a written route note naming (a) the existing forge entry points you will extend, (b) the noise functions you must add, (c) the one vehicle you will build.

### Step 1 — The reference brief (30 min)

Create `docs/references/vehicles/REFERENCE.md` — first-party, our own words, physically grounded, in the *format* of the diner's reference brief:

- Subject: one vehicle class, chosen for screen area in Nuke Town Rebuild. **Recommendation: the parked sedan/pickup family**, because HF-462 names "coach, truck, cars" and a parked vehicle is scenery (see `open-world-city-art-loop` §4.5), so it can ship without touching movement authority.
- Contents, every line cited or marked *(est.)*: overall dimensions and ride height; panel-gap width (mm) and shut-line depth; automotive paint as a two-layer stack (base coat albedo + clearcoat with its own roughness and "orange peel" normal at a stated spatial frequency); glass transmission/tint and the interior darkness behind it; tyre sidewall geometry and rubber roughness; chrome vs painted-steel Fresnel; and a **wear vocabulary** with heights and extents (stone-chip band at the leading edge, sill dirt line height above ground, sun-bleached horizontal surfaces, rust bloom at seams and wheel arches).
- A short **negative list** in the diner's spirit: no manufacturer badges, no grille trade dress, no model names, no readable plates.

**Gate:** every numeric line is either cited or marked *(est.)*; a reader who has never seen the vehicle could brief a builder from it alone.

### Step 2 — Reference plates + the compare harness (30 min)

- `scripts/qa/capture-vehicle-views.mjs` — headless capture of a fixed, named camera set (front three-quarter, rear three-quarter, side elevation, close detail on a wheel arch, close detail on a glass/pillar junction) at a declared viewport and DPR, writing `artifacts/<lane>/<round>/<view>.png` plus a `hud.json`. Model it on the existing `scripts/qa/capture-map3-views.mjs` / `capture-arena-viewpoints.mjs`; reuse the existing harness, do not invent a second one.
- `scripts/qa/critic-reference-sheet.mjs` — takes a capture directory and the reference brief, and emits **one HTML contact sheet** pairing each named view with its reference row and the brief's numbers for that view. This is the artefact a critic (human or vision model) is handed. Keep it a static file; `creative/visual-web-artifacts` is the pattern.
- Reference plates themselves: **first-party only**. If the owner has not supplied photographs, the sheet ships with the *written* brief in the reference column and a `PLATE: PENDING` marker — never a game frame, never an unlicensed photo.

**Gate:** `node scripts/qa/capture-vehicle-views.mjs` produces all five views headless with the ComfyUI/VRAM preflight respected; the contact sheet opens and shows five reference/capture pairs.

### Step 3 — The material library (45 min)

- New file `src/rendering/vehicle-surface-library.ts` — a set of `SurfaceDescription` functions built on the **existing** `surface-forge.ts` API: `automotivePaint({ base, clearcoatRoughness, orangePeelScale, chipDensity })`, `chromeTrim({ wear })`, `paintedSteelPanel({ dirtLineHeight })`, `tyreRubber()`, `autoGlass({ tint, transmission })`, `plasticTrim({ uvFade })`. Each returns `{ albedo, height, roughness, ao }` for a normalised `(u, v)`, and each derives its variation from the seeded `SurfaceNoise` — adding to `surface-forge.ts` only the noise functions Step 0 proved absent.
- Materials come out through `surfaceStandardMaterial(...)`. Where clearcoat is genuinely needed, use `MeshPhysicalNodeMaterial` — **check first** whether the art-path gate test in the target file permits it; if it does not, get the clearcoat look from roughness + the micro-detail tile rather than arguing with the gate.
- New file `src/rendering/vehicle-surface-library.test.ts` — determinism (same seed → byte-identical raster), texel budget within `surfaceTexelBudget()`, headless-safety (no throw with no DOM), and one assertion per surface that a named reference number from Step 1 is actually expressed in the description (e.g. the chip band occupies the stated fraction of the leading edge).

**Gate:** `npx tsc --noEmit` clean against a **baseline error list captured before you edited anything** (pre-existing errors are reported as pre-existing, never "fixed" under an additive mandate); `npx vitest run src/rendering/vehicle-surface-library.test.ts src/rendering/surface-forge.test.ts`.

### Step 4 — One bounded gauntlet round (20 min)

Run exactly **one** round of `visual-gauntlet-loop` against the frozen bar: capture → contact sheet → a fresh critic that is shown the sheet and the brief and nothing about how it was built → one largest gap, labelled by class (spec / implementation / camera-lighting / missing-evidence / performance) → one bounded correction → re-capture. Retain both captures so regression is visible. Record the round in the evidence directory with source SHA, viewport, DPR, renderer backend and the decision state.

**Gate:** the round terminates in an explicit state (`continue | refine-spec | refine-code | request-input | stop`) with the remaining budget written down. **Do not run a second round in this pass** — the ceiling is there to stop a plateau burning the session.

### Step 5 — Provenance and skill capture (20 min)

- Add **register row 51** (morning-diner, pinned `75c37de9b35f313ba9050f76b52674cb677e5491`, **licence: NO LICENSE FILE at the pinned revision**, decision: method-only, embedded in the new skill) and **row 52** (Astra threads, output-only bar) to `C:/Users/david/AppData/Local/hermes/.akephalos/references/ai-3d-technique-register.md`.
- Author `Skills/game-development/reference-grounded-asset-forge/SKILL.md` in the **canonical vault store** (never through the junction), carrying the §4.2(1) reference-brief format and critic contract, plus the budget table and the "the reference must be lawfully held" boundary.
- Write the paired **evaluation record** required by `skill-regression-policy.json`, and run the two guards manually since the cron is paused: `python scripts/technique_register_guard.py` and `python scripts/skill_regression_guard.py` from the AKP root.
- Add `docs/technique-studies/morning-diner-procedural-materials.md` with the three falsifiers from §4.2(2).

**Gate:** both AKP guards green; the register row names a carrying skill that exists and a hash-matched evaluation record.

### Step 6 — Handoff (10 min)

- `npm run pipeline:preflight` again; `npm run qa:pass65:owner-feedback` (HF-462 needs a ledger row that points at real evidence, and `IMPLEMENTED` is not `VERIFIED`).
- PR from the repository template naming machine, harness, base SHA, head SHA, changed paths, tests and release-note impact. **Do not merge it, do not publish, do not touch `gh-pages`.**
- Report separately: observations, inferences, assumptions, unknowns, falsifiers. Name what is still OPEN — at minimum the noise-core inventory answer from Step 0, whether `MeshPhysicalNodeMaterial` is permitted in the target art file, and whether first-party reference plates exist.

### Explicitly out of scope for this pass

Every remaining vehicle beyond the one family; the coach and the truck; any Blender or Blender-MCP work (that is test map 4, and only if the owner schedules it); installing any hub skill or npm package; ComfyUI generation of any kind; and the HF-463/464/465/467 gameplay items, which belong to other lanes.

---

## Appendix — sources

- <https://github.com/mrdoob/three.js/wiki/Three.js-Shading-Language> · <https://threejs.org/docs/TSL.html> · <https://threejs.org/examples/webgpu_tsl_procedural_terrain.html> · <https://github.com/mrdoob/three.js/blob/master/examples/webgpu_tsl_wood.html>
- <https://github.com/boytchev/tsl-textures> (MIT)
- <https://github.com/Auburn/FastNoiseLite> (MIT) · <https://github.com/stegu/webgl-noise> (MIT)
- <https://github.com/ahujasid/blender-mcp> (MIT)
- <https://somethingbig.ai/gauntlet-loop> (Matt Shumer, 2026-07-27)
- Skills Hub: `https://nousresearch.github.io/hermes-agent/docs/api/skills-meta.json`, `https://clawhub.ai/api/search?q=…`
- Local, read-only: `C:/Users/david/projects/morning-diner-ref` @ `75c37de9b35f313ba9050f76b52674cb677e5491`; `C:/Users/david/Documents/desky-bootstrap-clone/Skills/`; `C:/Users/david/AppData/Local/hermes/.akephalos/references/ai-3d-technique-register.md`; `C:/Users/david/projects/aa-claude-research/docs/PASS84_OWNER_FEEDBACK_2026-09-02.md`
